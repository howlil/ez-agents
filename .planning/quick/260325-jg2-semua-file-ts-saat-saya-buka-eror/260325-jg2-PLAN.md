# Plan: Fix TypeScript Errors

**Created:** 2026-03-25
**Mode:** quick
**Priority:** High (blocking development workflow)

## Summary

Fix 2911 TypeScript errors across 141 files to enable smooth development experience when opening files in the IDE.

## Error Categories

1. **Strict null checks** (most common) - `undefined` not assignable to `string`
2. **Possibly undefined access** - Object is possibly 'undefined'
3. **Unused variables** - Variables declared but never read
4. **Generic type constraints** - Type parameter issues in FP modules
5. **Implicit 'any' types** - Test files with missing type annotations

## Tasks

### Task 1: Fix bin/lib/ core library errors

**files:**
- `bin/lib/config.ts` (8 errors)
- `bin/lib/frontmatter.ts` (25 errors)
- `bin/lib/state.ts` (17 errors)
- `bin/lib/error-cache.ts` (1 error)
- `bin/lib/assistant-adapter.ts` (1 error)
- `bin/lib/session-chain.ts` (1 error)
- `bin/lib/milestone.ts` (1 error)

**action:**
Fix strict null check errors by:
- Adding proper null/undefined checks before using values
- Using nullish coalescing (`??`) or optional chaining (`?.`)
- Adding type assertions where safe
- Removing unused variables

**verify:**
- Run `npx tsc --noEmit` and confirm errors for these files are resolved
- Ensure runtime behavior is preserved (no new undefined errors)

**done:**
- [ ] All listed files compile without errors
- [ ] No new runtime issues introduced

---

### Task 2: Fix FP (Functional Programming) module type issues

**files:**
- `bin/lib/fp/transform.ts` (6 errors)
- `bin/lib/fp/pipe.ts` (4 errors)
- `bin/lib/fp/memoize.ts` (4 errors)
- `bin/lib/fp/immutable.ts` (1 error)
- `bin/lib/fp/index.ts` (1 error)

**action:**
Fix generic type constraint errors by:
- Adding proper generic type bounds
- Fixing type assertions with `unknown` intermediate where needed
- Resolving ambiguous exports
- Adding explicit type annotations for `this` context

**verify:**
- Run `npx tsc --noEmit` and confirm FP module errors are resolved
- Check that FP utilities still work correctly

**done:**
- [ ] All FP module files compile without errors
- [ ] Type safety maintained for generic functions

---

### Task 3: Fix test files and remaining bin/lib files

**files:**
- Test files with implicit 'any' types (141 files total, focusing on most critical first)
- Remaining `bin/lib/` files with errors

**action:**
For test files:
- Add explicit type annotations for variables (especially `tmpDir`)
- Fix parameter type annotations
- Handle possibly undefined values

For remaining bin/lib files:
- Fix null checks and unused variables
- Add proper type guards

**verify:**
- Run `npx tsc --noEmit` and confirm error count is reduced to zero or minimal
- Run tests to ensure functionality preserved

**done:**
- [ ] All test files compile without errors
- [ ] All bin/lib files compile without errors
- [ ] Full test suite passes

---

## Notes

- Focus on Task 1 first (highest impact, most errors in critical files)
- Task 2 requires careful handling of generic types
- Task 3 is bulk work but mostly mechanical (adding type annotations)
- Consider using TypeScript's `--strictNullChecks` flag selectively if needed
- Total estimated errors to fix: ~68 in core files (Tasks 1-2), remainder in tests (Task 3)

## Success Criteria

- `npx tsc --noEmit` completes with zero errors
- All files can be opened in IDE without TypeScript error popups
- Existing functionality preserved (tests pass)
