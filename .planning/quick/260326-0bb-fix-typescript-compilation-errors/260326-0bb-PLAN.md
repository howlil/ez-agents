# Quick Plan 260326-0bb: Fix TypeScript Compilation Errors

**Created:** 260326
**Type:** Quick Task - Blocker Resolution
**Mode:** Quick (standard)

---

## Analysis

**Total errors:** ~755 TypeScript errors

### Error Categories

1. **Adapter files** (~15 errors) - Token usage type mismatches
   - `ClaudeAdapter.ts`, `KimiAdapter.ts`, `OpenAIAdapter.ts`, `QwenAdapter.ts`
   - Issue: `usage` property undefined vs required, token property access

2. **Context modules** (~10 errors) - Missing imports, type mismatches
   - `context-compressor.ts` - Missing decorator imports
   - `context-manager.ts` - Type compatibility issues

3. **core.ts** (~3 errors) - AuditExecOptions, undefined types

4. **dependency-graph.ts** (~5 errors) - madge module, type issues

5. **deploy-runner.ts** (~5 errors) - spawn type annotations

6. **discussion-synthesizer.ts** (~4 errors) - undefined types

7. **file-lock.ts** (~2 errors) - dynamic import types

8. **finops/cost-reporter.ts** (~2 errors) - AggregateResult types

9. **framework-detector.ts** (~3 errors) - undefined object access

10. **frontmatter.ts** (~15 errors) - undefined yaml/current access

---

## Tasks

### Task 1: Fix Adapter Files

**Files:** `bin/lib/adapters/*.ts`
**Action:** Fix token usage types, add undefined handling
**Verify:** No more adapter errors
**Done:** Adapters type-check

### Task 2: Fix Context Modules

**Files:** `context-compressor.ts`, `context-manager.ts`
**Action:** Fix imports, add type assertions
**Verify:** No more context errors
**Done:** Context modules type-check

### Task 3: Fix Remaining Files

**Files:** `core.ts`, `dependency-graph.ts`, `deploy-runner.ts`, `discussion-synthesizer.ts`, `file-lock.ts`, `finops/cost-reporter.ts`, `framework-detector.ts`, `frontmatter.ts`
**Action:** Add null checks, fix types
**Verify:** `npm run typecheck` passes
**Done:** All TypeScript errors fixed

---

## Must Haves

- [ ] All ~755 TypeScript errors fixed
- [ ] `npm run typecheck` passes with 0 errors
- [ ] No breaking changes to public APIs
- [ ] Atomic commits per file group
- [ ] Summary documenting fixes

---

## Output

**Summary:** `.planning/quick/260326-0bb-fix-typescript-compilation-errors/260326-0bb-SUMMARY.md`
