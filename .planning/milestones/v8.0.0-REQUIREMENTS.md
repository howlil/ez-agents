# ez-agents v8.0.0 Requirements — Test Quality (100% Pass Rate)

**Milestone:** v8.0.0 Test Quality Improvement
**Created:** 2026-03-27
**Goal:** Fix all 104 failing tests to achieve 100% test pass rate

---

## Requirements Overview

**Total:** 38 requirements across 5 categories
- **Analytics:** 6 requirements (24 tests)
- **FinOps:** 6 requirements (23 tests)
- **Context Modules:** 8 requirements (20 tests)
- **Core Modules:** 10 requirements (25 tests)
- **Integration:** 8 requirements (12 tests)

---

## ANALYTICS Requirements (6)

### ANALYTICS-01: NPSTracker Implementation
**Status:** ❌ Not started
**Tests:** 3 failing
- `recordResponse()` categorizes promoters, passives, detractors
- `calculateScore()` returns NPS = %promoters - %detractors
- `getTrend()` returns NPS change over time periods

### ANALYTICS-02: AnalyticsCollector Implementation
**Status:** ❌ Not started
**Tests:** 4 failing
- `track()` records event with timestamp and metadata
- `startSession()` creates session with unique ID
- `endSession()` closes session with duration
- `getEvents()` returns filtered events by name

### ANALYTICS-03: AnalyticsReporter Implementation
**Status:** ❌ Not started
**Tests:** 4 failing
- `generateReport()` creates summary with key metrics
- `aggregateMetrics()` combines data from multiple sources
- `exportReport()` writes report to file in specified format
- `scheduleReport()` creates recurring report configuration

### ANALYTICS-04: CohortAnalyzer Implementation
**Status:** ❌ Not started
**Tests:** 5 failing
- `defineCohort()` creates cohort by signup period
- `addUserToCohort()` assigns user based on signup date
- `calculateRetention()` returns retention rate per period
- `compareCohorts()` returns comparative retention metrics
- `getCohortMetrics()` returns size, activity, and lifetime value

### ANALYTICS-05: FunnelAnalyzer Implementation
**Status:** ❌ Not started
**Tests:** 5 failing
- `defineFunnel()` creates funnel with ordered steps
- `trackConversion()` records user progression through funnel
- `getConversionRates()` returns percentage at each step
- `getDropOffPoints()` identifies biggest conversion losses
- `compareFunnels()` returns comparative metrics between funnels

### ANALYTICS-06: Analytics CLI Tests
**Status:** ❌ Not started
**Tests:** 5 failing
- `analytics track --event` records event with properties
- `analytics session --start` creates new session
- `analytics session --end` closes session with duration
- `analytics report --type` generates report in specified format
- `analytics export --format csv` exports data to file

---

## FINOPS Requirements (6)

### FINOPS-01: BudgetEnforcer Implementation
**Status:** ❌ Not started
**Tests:** 7 failing
- `setBudget()` configures spending limit with optional warning threshold
- `checkBudget()` returns status based on current spending
- `checkBudget()` returns warning when spending exceeds threshold
- `checkBudget()` returns exceeded when spending exceeds ceiling
- `enforce()` blocks operation when budget exceeded
- `enforce()` allows operation when within budget
- `getSpendingByCategory()` returns breakdown of spending

### FINOPS-02: CostReporter Implementation
**Status:** ❌ Not started
**Tests:** 5 failing
- `generateReport()` creates cost summary with breakdowns
- `getCostByService()` returns costs grouped by service
- `getCostByPeriod()` returns costs grouped by time period
- `exportReport()` writes report to file in specified format
- `comparePeriods()` returns cost comparison between two periods

### FINOPS-03: FinOpsAnalyzer Implementation
**Status:** ❌ Not started
**Tests:** 5 failing
- `analyzeCosts()` returns breakdown by service and trend
- `getOptimizationRecommendations()` returns actionable cost savings
- `detectAnomalies()` identifies unusual spending patterns
- `forecastSpending()` predicts future costs based on historical data
- `getCostPerUnit()` calculates efficiency metrics

### FINOPS-04: SpotManager Implementation
**Status:** ❌ Not started
**Tests:** 4 failing
- `requestSpotInstance()` provisions spot instance with max price
- `handleInterruption()` gracefully handles spot instance termination
- `getSpotSavings()` calculates savings vs on-demand pricing
- `getOptimalSpotConfig()` recommends best spot instance configuration

### FINOPS-05: FinOps CLI Tests
**Status:** ❌ Not started
**Tests:** 6 failing
- `finops budget --set` configures spending limit
- `finops budget --status` shows current spending status
- `finops record --cost` logs expense with category
- `finops report --period` generates cost report
- `finops analyze --recommendations` returns optimization suggestions
- `finops export --format csv` exports cost data

### FINOPS-06: Circuit Breaker Integration Tests
**Status:** ❌ Not started
**Tests:** 4 failing
- State transitions: rejects requests when OPEN
- CircuitBreakerAdapter creates adapter with correct configuration
- CircuitBreakerAdapter wraps function calls with circuit breaker
- Integration with circuit breaker system

---

## CONTEXT Requirements (8)

### CONTEXT-01: ArchetypeDetector Tests
**Status:** ❌ Not started
**Tests:** 3 failing (parse errors)
- Fix test syntax errors
- Fix archetype detection tests
- Fix confidence score tests

### CONTEXT-02: BusinessFlowMapper Tests
**Status:** ❌ Not started
**Tests:** 3 failing (parse errors)
- Fix test syntax errors
- Fix `analyzeDataFlow()` tests
- Fix `findIntegrationPoints()` tests

### CONTEXT-03: CodebaseAnalyzer Tests
**Status:** ❌ Not started
**Tests:** 3 failing (parse errors)
- Fix test syntax errors
- Fix `classifyFile()` tests
- Fix entry point detection tests

### CONTEXT-04: ConcernsReport Tests
**Status:** ❌ Not started
**Tests:** 2 failing (parse errors)
- Fix test syntax errors
- Fix severity assignment tests

### CONTEXT-05: ConstraintExtractor Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### CONTEXT-06: DependencyGraph Tests
**Status:** ❌ Not started
**Tests:** 2 failing (parse errors)
- Fix test syntax errors
- Fix graph construction tests

### CONTEXT-07: FrameworkDetector Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### CONTEXT-08: StackDetector & TechDebt Tests
**Status:** ❌ Not started
**Tests:** 5 failing (parse errors)
- Fix test syntax errors
- Fix stack detection tests
- Fix tech debt analyzer tests

---

## CORE Requirements (10)

### CORE-01: Commands Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### CORE-02: Config Tests
**Status:** ❌ Not started
**Tests:** 17 failing
- `config-ensure-section` creates config.json with expected structure
- `config-ensure-section` is idempotent
- `config-ensure-section` detects Brave Search from file-based key
- `config-ensure-section` merges user defaults from defaults.json
- `config-ensure-section` merges nested workflow keys
- `config-set` sets top-level string value
- `config-set` coerces true/false to boolean
- `config-set` coerces numeric strings to numbers
- `config-set` preserves plain strings
- `config-set` sets nested values via dot-notation
- `config-set` auto-creates nested objects
- `config-get` gets top-level value
- `config-get` gets nested value via dot-notation
- `config-get` errors for nonexistent key
- `config-get` errors for nested nonexistent key
- `config-get` errors when config.json does not exist

### CORE-03: Content Scanner Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### CORE-04: Context Manager Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### CORE-05: Core Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### CORE-06: Dispatcher Tests
**Status:** ❌ Not started
**Tests:** 12 failing
- `--cwd=` form overrides working directory
- `--cwd=` with empty value produces error
- `--cwd` with invalid path produces error
- `template` unknown subcommand errors
- `phases` unknown subcommand errors
- `todo` unknown subcommand errors
- `init` unknown workflow errors
- `find-phase` locates phase directory by number
- `roadmap update-plan-progress` updates phase progress
- `state` with no subcommand calls cmdStateLoad
- `summary-extract` parses SUMMARY.md frontmatter

### CORE-07: File Access Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### CORE-08: File Lock Timeout Tests
**Status:** ❌ Not started
**Tests:** 1 failing
- LOCK-02: second lock attempt times out with deterministic error

### CORE-09: Health Route Tests
**Status:** ❌ Not started
**Tests:** 1 failing
- Health route exposes payload with status, checks, and timestamp

### CORE-10: Planning Write Temp Tests
**Status:** ❌ Not started
**Tests:** 2 failing
- Creates temp file in target directory and cleans it up
- Cleans up staged temp artifact when rename fails

---

## INTEGRATION Requirements (8)

### INTEGRATION-01: E2E Workflow Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### INTEGRATION-02: Foundation Logging Integration
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### INTEGRATION-03: Frontmatter CLI Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### INTEGRATION-04: Verify Tests
**Status:** ❌ Not started
**Tests:** 1 failing (parse errors)
- Fix test syntax errors

### INTEGRATION-05: Roadmap Analyze Tests
**Status:** ❌ Not started
**Tests:** 9 failing
- Parses phases with goals and disk status
- Extracts goals and dependencies
- Extracts goals with colon outside bold format
- Handles mixed colon formats
- Returns researched status for RESEARCH.md
- Returns discussed status for CONTEXT.md
- Returns empty status for no recognized files
- Extracts milestone headings and version numbers
- Detects checklist-only phases missing details

### INTEGRATION-06: Roadmap Missing Phase Details
**Status:** ❌ Not started
**Tests:** 1 failing
- Detects missing phase details

### INTEGRATION-07: Roadmap Update Plan Progress
**Status:** ❌ Not started
**Tests:** 6 failing
- Missing phase number returns error
- Nonexistent phase returns error
- No plans found returns updated false
- Updates progress for partial completion
- Updates progress and checks checkbox on completion
- Missing ROADMAP.md returns updated false

### INTEGRATION-08: Remaining Unit Tests
**Status:** ❌ Not started
**Tests:** 16 failing (parse errors)
- Fix learning-tracker.test.ts
- Fix milestone.test.ts
- Fix phase.test.ts
- Fix quality-detector.test.ts
- Fix quality-gate.test.ts
- Fix rca-engine.test.ts
- Fix revision-loop.test.ts
- Fix security-fixes.test.ts
- Fix skill-resolver.test.ts
- Fix skill-validator.test.ts
- Fix state.test.ts
- Fix task-formatter.test.ts
- Fix timeout-exec.test.ts
- Fix tradeoff-analyzer.test.ts
- Fix url-fetch.test.ts
- Fix verify-health.test.ts

---

## Acceptance Criteria

**All requirements must:**
- [ ] Pass all associated tests
- [ ] Have no TypeScript errors
- [ ] Maintain existing passing tests
- [ ] Follow project coding conventions
- [ ] Include proper error handling
- [ ] Have appropriate test coverage

**Milestone complete when:**
- [ ] 38/38 requirements complete
- [ ] 307/307 tests passing (100%)
- [ ] 0 TypeScript errors
- [ ] Test coverage ≥ 70%
- [ ] All parse errors fixed
- [ ] All implementations complete

---

## Priority Order

1. **Phase 19 (ANALYTICS)** - 24 tests, foundational analytics
2. **Phase 20 (FINOPS)** - 23 tests, financial operations
3. **Phase 21 (CONTEXT)** - 20 tests, context modules
4. **Phase 22 (CORE)** - 25 tests, core functionality
5. **Phase 23 (INTEGRATION)** - 12 tests, integration validation

---

*Last updated: 2026-03-27*
