---
phase: "45"
plan: "01"
type: "implementation"
wave: 1
depends_on: []
files_modified:
  - bin/lib/phase.cjs
  - bin/lib/frontmatter.cjs
  - ez-agents/workflows/execute-phase.md
  - .planning/config.json
autonomous: true
must_haves:
  artifacts:
    - path: bin/lib/phase.cjs
      provides: Dynamic wave computation from depends_on
    - path: bin/lib/frontmatter.cjs
      provides: Optional wave field validation
    - path: ez-agents/workflows/execute-phase.md
      provides: maxParallel batching and failure handling
    - path: .planning/config.json
      provides: maxParallel configuration option
  key_links:
    - path: bin/lib/task-graph.cjs
      provides: Existing topologicalGenerations() for wave computation
    - path: .planning/REQUIREMENTS.md
      provides: WAVE-01, WAVE-02, WAVE-03 requirement definitions
requirements:
  - WAVE-01
  - WAVE-02
  - WAVE-03
---

<objective>
Replace static wave assignment with dynamic computation, add resource-aware parallelism, and implement failure handling.
</objective>

<task id="1" depends_on="[]">
<read_first>
- bin/lib/phase.cjs — cmdPhasePlanIndex() function (around line 261) that groups plans by wave
- bin/lib/frontmatter.cjs — FRONTMATTER_SCHEMAS.plan for plan validation schema
- bin/lib/task-graph.cjs — TaskGraph.computeWaves() using topologicalGenerations()
- .planning/config.json — Current configuration structure
</read_first>

<acceptance_criteria>
- [ ] bin/lib/phase.cjs line ~261: Replaces `const wave = parseInt(fm.wave, 10) || 1;` with TaskGraph-based computation
- [ ] bin/lib/phase.cjs: Contains `new TaskGraph()` or `TaskGraph()` constructor call in cmdPhasePlanIndex()
- [ ] bin/lib/phase.cjs: Contains `.computeWaves()` method call in cmdPhasePlanIndex()
- [ ] bin/lib/frontmatter.cjs line ~228: FRONTMATTER_SCHEMAS.plan.required does NOT include 'wave' string
- [ ] .planning/config.json: Contains `"maxParallel"` key with integer value
- [ ] ez-agents/workflows/execute-phase.md: Contains "maxParallel" string in workflow body
- [ ] ez-agents/workflows/execute-phase.md: Contains "blocked" string in failure handling context
</acceptance_criteria>

<action>
1. **Update bin/lib/frontmatter.cjs** — Make wave field optional:
   - Locate FRONTMATTER_SCHEMAS.plan.required array (around line 228)
   - Change from: `['phase', 'plan', 'type', 'wave', 'depends_on', 'files_modified', 'autonomous', 'must_haves']`
   - To: `['phase', 'plan', 'type', 'depends_on', 'files_modified', 'autonomous', 'must_haves']` (remove 'wave')

2. **Update bin/lib/phase.cjs** — Compute waves dynamically in cmdPhasePlanIndex():
   - Locate line ~261: `const wave = parseInt(fm.wave, 10) || 1;`
   - Replace with: Build task array from plans, create TaskGraph, call computeWaves(), map results back to plans
   - Code pattern:
     ```
     const tasks = plans.map(p => ({ id: p.id, dependencies: p.depends_on || [] }));
     const taskGraph = new TaskGraph();
     taskGraph.buildDAG(tasks);
     const waves = taskGraph.computeWaves();
     // Map wave results: for each wave index i, for each planId in waves[i], set plan.wave = i + 1
     ```

3. **Update .planning/config.json** — Add maxParallel configuration:
   - Add to workflow section: `"maxParallel": 5` (default limit for concurrent agents)

4. **Update ez-agents/workflows/execute-phase.md** — Add resource-aware parallelism:
   - In execute_waves step, add batch computation logic:
     ```
     **Batch execution (resource-aware):**
     Read maxParallel from config (default: 5). If wave has {N} plans:
     - Batch size = maxParallel
     - Number of batches = Math.ceil(N / maxParallel)
     - Execute each batch sequentially, wait for all agents in batch to complete before next batch
     ```

5. **Update ez-agents/workflows/execute-phase.md** — Add failure handling:
   - Add failure classification section:
     ```
     **Failure classification:**
     - Transient (API timeout, rate limit, network error): Auto-retry up to 2 times with exponential backoff
     - Permanent (logic error, missing file, validation failure): Stop, report to user
     ```
   - Add dependency-aware failure propagation:
     ```
     **Dependency-aware failure handling:**
     1. Collect failed plan IDs from current wave
     2. For each plan in next wave: check if depends_on includes any failed plan
     3. If yes: mark plan as "blocked" (skip execution, report in summary)
     4. If no: execute normally
     ```
</action>
</task>

<task id="2" depends_on="1">
<read_first>
- ez-agents/workflows/execute-phase.md — Full workflow structure for checkpoint_handling and failure_handling sections
- .planning/REQUIREMENTS.md — WAVE-01, WAVE-02, WAVE-03 requirement definitions
- bin/lib/phase.cjs — Verify cmdPhasePlanIndex() changes from Task 1
</read_first>

<acceptance_criteria>
- [ ] ez-agents/workflows/execute-phase.md: Contains "Math.ceil" or "ceil" for batch computation
- [ ] ez-agents/workflows/execute-phase.md: Contains "Transient" and "Permanent" failure classification
- [ ] ez-agents/workflows/execute-phase.md: Contains "blocked" keyword in dependency handling context
- [ ] ez-agents/workflows/execute-phase.md: Contains "retry" keyword for transient failures
- [ ] ez-agents/workflows/execute-phase.md: Contains "exponential backoff" for retry logic
</acceptance_criteria>

<action>
1. **Implement batch execution loop** in ez-agents/workflows/execute-phase.md execute_waves step:
   - Insert after "For each wave:" section
   - Add batch computation using Math.ceil(wavePlans.length / maxParallel)
   - Loop through batches, spawn min(remaining, maxParallel) agents per batch
   - Wait for batch completion before next batch

2. **Implement failure classification** in failure_handling section:
   - Add section distinguishing transient vs permanent failures
   - Transient: API timeout, rate limit (429), network error → retry up to 2 times
   - Permanent: logic error, missing file, validation failure → stop and report

3. **Implement dependency-aware blocking** in execute_waves step:
   - After each wave completes, collect failed plan IDs
   - Before starting next wave, filter out plans whose depends_on includes failed plans
   - Mark filtered plans as "blocked" in summary output
   - Continue with non-blocked plans

4. **Update summary aggregation** to show blocked plans:
   - Add "Blocked" column to status table
   - List blocked plans in Issues Encountered section with reason
</action>
</task>

<task id="3" depends_on="1,2">
<read_first>
- bin/lib/phase.cjs — Verify cmdPhasePlanIndex() changes
- ez-agents/workflows/execute-phase.md — Verify execute_waves and failure_handling changes
- .planning/REQUIREMENTS.md — Verify WAVE-01, WAVE-02, WAVE-03 coverage
</read_first>

<acceptance_criteria>
- [ ] bin/lib/phase.cjs: grep for "computeWaves()" returns match in cmdPhasePlanIndex()
- [ ] bin/lib/frontmatter.cjs: grep for "FRONTMATTER_SCHEMAS.plan.required" does NOT contain 'wave' as required field
- [ ] .planning/config.json: grep for "maxParallel" returns integer value
- [ ] ez-agents/workflows/execute-phase.md: grep for "blocked" returns match in failure context
- [ ] ez-agents/workflows/execute-phase.md: grep for "Transient" returns match
- [ ] ez-agents/workflows/execute-phase.md: grep for "exponential backoff" returns match
</acceptance_criteria>

<action>
1. **Verify dynamic wave computation** (WAVE-01):
   - Run: `grep "computeWaves()" bin/lib/phase.cjs` — should match in cmdPhasePlanIndex()
   - Run: `grep "new TaskGraph" bin/lib/phase.cjs` — should match in cmdPhasePlanIndex()
   - Run: `grep "'wave'" bin/lib/frontmatter.cjs` — should NOT appear in required array

2. **Verify resource-aware parallelism** (WAVE-02):
   - Run: `grep "maxParallel" .planning/config.json` — should return integer
   - Run: `grep "Math.ceil" ez-agents/workflows/execute-phase.md` — should match batch logic

3. **Verify failure handling** (WAVE-03):
   - Run: `grep "blocked" ez-agents/workflows/execute-phase.md` — should match in dependency context
   - Run: `grep "Transient" ez-agents/workflows/execute-phase.md` — should match failure classification
   - Run: `grep "exponential backoff" ez-agents/workflows/execute-phase.md` — should match retry logic

4. **Document test scenarios** (for manual execution verification):
   - Scenario A: Basic chain — Plan 01 (no deps) → Wave 1, Plan 02 (depends 01) → Wave 2
   - Scenario B: Parallel — Plan 01, 02, 03 (no deps) → all Wave 1
   - Scenario C: Diamond — Plan 01 → Wave 1, Plan 02/03 (depend 01) → Wave 2, Plan 04 (depends 02,03) → Wave 3
   - Scenario D: Failure — Wave 1 Plan 01 fails → Wave 2 plans depending on 01 are blocked
</action>
</task>

<verification_criteria>
1. **Dynamic wave computation (WAVE-01):**
   - Plans grouped by wave based on depends_on, not static wave field
   - TaskGraph.computeWaves() called in cmdPhasePlanIndex()
   - Wave field in frontmatter is optional (not in required schema)

2. **Resource-aware parallelism (WAVE-02):**
   - maxParallel config exists in .planning/config.json
   - execute-phase.md batches wave execution based on maxParallel
   - Batch size limits concurrent agent spawns

3. **Failure handling (WAVE-03):**
   - Failure classification (transient vs permanent) documented
   - Dependency-aware blocking: failed plans block dependents
   - aggregate_results shows blocked plans separately
   - Retry logic for transient failures

4. **Backward compatibility:**
   - Existing plans with static wave field still work (wave value ignored, computed from depends_on)
   - Plans without wave field work correctly
</verification_criteria>
