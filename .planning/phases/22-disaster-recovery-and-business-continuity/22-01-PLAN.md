---
phase: 22-disaster-recovery-and-business-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/recovery-backup.test.cjs
  - ez-agents/bin/lib/backup-service.cjs
  - ez-agents/bin/lib/recovery-manager.cjs
  - ez-agents/bin/lib/config.cjs
  - ez-agents/bin/ez-tools.cjs
  - commands/ez/backup.md
autonomous: true
requirements: [RECOVER-01]
must_haves:
  truths:
    - "Backups write to `.planning/recovery/backups/<backup-id>/` with a `manifest.json` containing backup_id, created_at, scope, commit_sha, and per-file checksum entries"
    - "Recovery config lives under top-level `recovery` in `.planning/config.json` and includes `backup_scope` plus retention"
    - "RecoveryManager delegates backup creation to BackupService and ez-tools reuses that same path"
  artifacts:
    - path: "ez-agents/bin/lib/backup-service.cjs"
      provides: "BackupService class"
      contains: "class BackupService"
    - path: "commands/ez/backup.md"
      provides: "/ez:backup command"
      contains: "name: ez:backup"
  key_links:
    - from: "ez-agents/bin/lib/backup-service.cjs"
      to: "ez-agents/bin/lib/planning-write.cjs"
      via: "safePlanningWriteSync stores manifests atomically"
      pattern: "safePlanningWriteSync"
---

<objective>
Build the core repository backup layer for Phase 22.

Output: `backup-service.cjs`, `recovery-manager.cjs`, config support, CLI wiring, `/ez:backup`, and backup tests.
</objective>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/22-disaster-recovery-and-business-continuity/22-RESEARCH.md
@.planning/phases/22-disaster-recovery-and-business-continuity/22-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 backup tests</name>
  <files>tests/recovery-backup.test.cjs</files>
  <read_first>
    - tests/helpers.cjs
    - tests/planning-write-temp.test.cjs
    - tests/verify-health.test.cjs
    - .planning/phases/22-disaster-recovery-and-business-continuity/22-VALIDATION.md
  </read_first>
  <action>
    Create `tests/recovery-backup.test.cjs` in the repo's Node test style.

    Cover these exact behaviors:
    - `BackupService.createBackup()` copies only configured recovery paths and skips missing optional paths
    - backups land in `.planning/recovery/backups/<backup-id>/`
    - `manifest.json` contains `backup_id`, `created_at`, `scope`, `commit_sha`, and `files`
    - file entries contain `path`, `sha256`, and `size_bytes`
    - retention pruning respects `recovery.retention.local_backups`
    - verification fails on checksum mismatch
  </action>
  <acceptance_criteria>
    - `tests/recovery-backup.test.cjs` exists
    - `tests/recovery-backup.test.cjs` contains `BackupService`
    - `tests/recovery-backup.test.cjs` contains `manifest.json`
    - `tests/recovery-backup.test.cjs` contains `local_backups`
    - `node --test tests/recovery-backup.test.cjs` runs before implementation and fails only for missing implementation
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: BackupService and recovery config</name>
  <files>ez-agents/bin/lib/backup-service.cjs, ez-agents/bin/lib/config.cjs</files>
  <read_first>
    - ez-agents/bin/lib/planning-write.cjs
    - ez-agents/bin/lib/config.cjs
    - ez-agents/bin/lib/logger.cjs
    - tests/recovery-backup.test.cjs
  </read_first>
  <action>
    Create `ez-agents/bin/lib/backup-service.cjs` exporting `BackupService` with:
    - `constructor(cwd, options = {})`
    - `createBackup({ label = 'manual', scope, verify = false } = {})`
    - `verifyBackup(backupDir)`
    - `pruneBackups(maxBackups)`

    Create backups under `.planning/recovery/backups/<timestamp>-<label>/`.
    Default `recovery.backup_scope` to `.planning`, `.github/workflows`, `commands`, `ez-agents/bin/lib`, `package.json`, and `package-lock.json`.
    Use `safePlanningWriteSync` for manifests and `Logger` for logging.

    Update `config.cjs` so top-level `recovery` supports:
    - `enabled`
    - `backup_scope`
    - `retention.local_backups`
    - `retention.drill_reports`
    - `rto_minutes.planning_state`
    - `rpo_minutes.planning_state`
  </action>
  <acceptance_criteria>
    - `grep -n "class BackupService" ez-agents/bin/lib/backup-service.cjs` returns a match
    - `grep -n "createBackup" ez-agents/bin/lib/backup-service.cjs` returns a match
    - `grep -n "verifyBackup" ez-agents/bin/lib/backup-service.cjs` returns a match
    - `grep -n "backup_scope" ez-agents/bin/lib/config.cjs` returns a match
    - `grep -n "local_backups" ez-agents/bin/lib/config.cjs` returns a match
    - `node --test tests/recovery-backup.test.cjs` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RecoveryManager, CLI wiring, and /ez:backup</name>
  <files>ez-agents/bin/lib/recovery-manager.cjs, ez-agents/bin/ez-tools.cjs, commands/ez/backup.md</files>
  <read_first>
    - ez-agents/bin/ez-tools.cjs
    - commands/ez/git-workflow.md
    - commands/ez/health.md
    - ez-agents/bin/lib/backup-service.cjs
  </read_first>
  <action>
    Create `ez-agents/bin/lib/recovery-manager.cjs` exporting `RecoveryManager` with `backup`, `listBackups`, and `verifyBackup`.

    Update `ez-tools.cjs` to add a `recovery` command with subcommands:
    - `backup`
    - `list-backups`
    - `verify-backup`

    Create `commands/ez/backup.md` with frontmatter:
    `name: ez:backup`
    `description: Create, list, and verify EZ Agents recovery backups`
    `argument-hint: "[create|list|verify] [--label <name>] [--backup <id-or-path>]"`
  </action>
  <acceptance_criteria>
    - `grep -n "class RecoveryManager" ez-agents/bin/lib/recovery-manager.cjs` returns a match
    - `grep -n "listBackups" ez-agents/bin/lib/recovery-manager.cjs` returns a match
    - `grep -n "verifyBackup" ez-agents/bin/lib/recovery-manager.cjs` returns a match
    - `grep -n "name: ez:backup" commands/ez/backup.md` returns a match
    - `grep -n "list-backups\\|verify-backup\\|backup" ez-agents/bin/ez-tools.cjs` returns matches for recovery subcommands
    - `node ez-agents/bin/ez-tools.cjs recovery backup --label smoke` creates a backup directory and exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
```bash
node --test tests/recovery-backup.test.cjs
npm test
```
</verification>
