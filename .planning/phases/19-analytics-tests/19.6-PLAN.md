---
ez_plan_version: 1.0
phase: 19
plan: 19.6
milestone: v8.0
wave: 3
depends_on:
  - 19.2
  - 19.3
  - 19.4
  - 19.5
files_modified:
  - bin/lib/commands/analytics-cli.ts
  - tests/analytics/analytics-cli.test.ts
requirements:
  - ANALYTICS-06
autonomous: true
---

# Phase 19.6: Fix Analytics CLI Tests

**Goal:** Create analytics CLI handler to fix 5 failing integration tests.

**Requirements:** ANALYTICS-06
**Tests:** 5 failing → 5 passing
**Wave:** 3 (depends on all analytics implementations from 19.2-19.5)

---

## Context

The analytics CLI commands need a handler to process CLI arguments and invoke the analytics classes. Tests expect commands to run without crashing.

**Failing Tests:**
1. `analytics track --event` records event with properties
2. `analytics session --start` creates new session
3. `analytics session --end` closes session with duration
4. `analytics report --type` generates report in specified format
5. `analytics export --format csv` exports data to file

---

## Must Haves

- [ ] CLI handler file exists at bin/lib/commands/analytics-cli.ts
- [ ] `analytics track --event=<name> --user=<id> --props=<json>` invokes AnalyticsCollector.track()
- [ ] `analytics session --start --user=<id>` invokes AnalyticsCollector.startSession() and outputs session ID
- [ ] `analytics session --end --id=<sessionId>` invokes AnalyticsCollector.endSession()
- [ ] `analytics report --type=<type> --format=<format>` invokes AnalyticsReporter.generateReport()
- [ ] `analytics export --format=<format> --output=<filename>` invokes AnalyticsReporter.exportReport()
- [ ] All 5 CLI tests pass (commands don't crash)
- [ ] No TypeScript errors introduced

---

## Tasks

### Task 1: Read Test Files and Existing CLI Patterns

<read_first>
- tests/analytics/analytics-cli.test.ts (test expectations)
- bin/lib/commands/ (existing CLI command patterns)
- .planning/phases/19-analytics-tests/19-RESEARCH.md (CLI command structure, section 2.6)
</read_first>

<action>
1. Read tests/analytics/analytics-cli.test.ts to identify exact CLI command expectations:
   - analytics track --event=page_view --user=user-123 --props={"page":"/home"}
   - analytics session --start --user=user-456
   - analytics session --end --id=<session-id>
   - analytics report --type=weekly --format=json
   - analytics export --format=csv --output=analytics-export
2. Check bin/lib/commands/ directory for existing CLI command patterns
3. Note how other CLI commands are structured (argument parsing, error handling)
4. Identify the main CLI entry point to understand how subcommands are routed
</action>

<acceptance_criteria>
- File tests/analytics/analytics-cli.test.ts exists and contains 5 test blocks
- Test file shows track command expects --event, --user, --props arguments
- Test file shows session --start expects --user argument and outputs session ID
- Test file shows session --end expects --id argument
- Test file shows report command expects --type and --format arguments
- Test file shows export command expects --format and --output arguments
- Directory bin/lib/commands/ contains at least one existing CLI command file for reference
- Existing CLI files show argument parsing pattern (e.g., parsing --key=value format)
</acceptance_criteria>

### Task 2: Create Analytics CLI Handler

<read_first>
- bin/lib/commands/ (existing CLI command file for reference pattern)
- tests/analytics/analytics-cli.test.ts (CLI test expectations)
</read_first>

<action>
Create bin/lib/commands/analytics-cli.ts with these exact behaviors:

1. Export function: `export async function handleAnalytics(args: string[], cwd: string): Promise<void>`
2. Parse subcommand from args[0]: 'track', 'session', 'report', 'export'
3. Parse arguments in format --key=value from remaining args

Implement track subcommand:
1. Parse --event, --user, --props arguments
2. Create AnalyticsCollector instance with cwd
3. Call collector.track({ name: event, userId: user, properties: JSON.parse(props) })
4. Output: "Event recorded: {event}"

Implement session subcommand:
1. Check for --start or --end flag
2. For --start:
   - Parse --user argument
   - Create AnalyticsCollector instance
   - Call collector.startSession({ userId: user })
   - Output: "Session ID: {sessionId}"
3. For --end:
   - Parse --id argument
   - Create AnalyticsCollector instance
   - Call collector.endSession(sessionId)
   - Output: "Session ended"

Implement report subcommand:
1. Parse --type, --format arguments
2. Create AnalyticsReporter instance
3. Call reporter.generateReport({ type, startDate, endDate })
4. Output report summary

Implement export subcommand:
1. Parse --format, --output arguments
2. Create AnalyticsReporter instance
3. Call reporter.exportReport(report, { format, filename: output })
4. Output: "Exported to: {outputPath}"
</action>

<acceptance_criteria>
- File bin/lib/commands/analytics-cli.ts exists
- File exports function handleAnalytics(args: string[], cwd: string): Promise<void>
- handleAnalytics parses subcommand from first argument
- handleAnalytics has case for 'track' subcommand that calls AnalyticsCollector.track()
- handleAnalytics has case for 'session' subcommand with --start and --end options
- handleAnalytics has case for 'report' subcommand that calls AnalyticsReporter.generateReport()
- handleAnalytics has case for 'export' subcommand that calls AnalyticsReporter.exportReport()
- CLI handler imports AnalyticsCollector from '../analytics/analytics-collector.js'
- CLI handler imports AnalyticsReporter from '../analytics/analytics-reporter.js'
</acceptance_criteria>

### Task 3: Register Analytics Command in Main CLI

<read_first>
- bin/lib/commands/ (find main CLI entry point, likely ez-tools.ts or commands.ts)
- bin/ez-tools.ts or bin/lib/commands/index.ts (CLI routing)
</read_first>

<action>
1. Find the main CLI entry point that routes subcommands
2. Add 'analytics' command handler that calls handleAnalytics()
3. Ensure the command routing passes args and cwd correctly

Typical pattern:
```typescript
case 'analytics':
  await handleAnalytics(args.slice(1), cwd);
  break;
```

4. Import handleAnalytics from './analytics-cli.js'
</action>

<acceptance_criteria>
- Main CLI file imports handleAnalytics from './analytics-cli.js' or equivalent path
- Main CLI file has case or route for 'analytics' subcommand
- analytics route passes remaining args (args.slice(1)) to handleAnalytics
- analytics route passes cwd parameter
- Command: node dist/bin/ez-tools.js analytics --help does not throw (if help implemented)
</acceptance_criteria>

### Task 4: Run CLI Tests and Verify

<read_first>
- tests/analytics/analytics-cli.test.ts (test expectations)
- tests/helpers.ts (runEzTools helper function)
</read_first>

<action>
Run CLI tests:
1. Run: npm test -- tests/analytics/analytics-cli.test.ts
2. Verify all 5 tests pass
3. If tests fail, check:
   - CLI handler is properly imported in main entry point
   - Argument parsing matches test expectations
   - Analytics classes are instantiated with correct cwd
4. Run: npm run typecheck to verify no TypeScript errors

Manual verification (optional):
1. Create temp directory
2. Run: node dist/bin/ez-tools.js analytics track --event=test --user=user1
3. Check .planning/analytics.json is created with event
</action>

<acceptance_criteria>
- Test command: npm test -- tests/analytics/analytics-cli.test.ts exits with code 0
- Test output shows "5 passed" or similar (all analytics CLI tests pass)
- Test output shows no "command not found" or "analytics is not recognized" errors
- Command: npm run typecheck exits with code 0 and shows "0 errors"
- Optional: Manual test creates .planning/analytics.json with expected event data
</acceptance_criteria>

---

## Verification

**Run Tests:**
```bash
npm test -- tests/analytics/analytics-cli.test.ts
```

**Expected Output:**
```
✓ tests/analytics/analytics-cli.test.ts > ez-agents analytics > analytics track --event records event with properties
✓ tests/analytics/analytics-cli.test.ts > ez-agents analytics > analytics session --start creates new session
✓ tests/analytics/analytics-cli.test.ts > ez-agents analytics > analytics session --end closes session with duration
✓ tests/analytics/analytics-cli.test.ts > ez-agents analytics > analytics report --type generates report in specified format
✓ tests/analytics/analytics-cli.test.ts > ez-agents analytics > analytics export --format csv exports data to file

Test Files  1 passed (1)
     Tests  5 passed (5)
```

**TypeScript Check:**
```bash
npm run typecheck
```

**Expected:** 0 errors

---

*Created: 2026-03-27*
*Status: Not started*
