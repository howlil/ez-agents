---
ez_plan_version: 1.0
phase: 19
plan: 19.3
milestone: v8.0
wave: 1
depends_on: []
files_modified:
  - bin/lib/analytics/analytics-reporter.ts
  - tests/analytics/analytics-reporter.test.ts
requirements:
  - ANALYTICS-03
autonomous: true
---

# Phase 19.3: Implement AnalyticsReporter Methods

**Goal:** Implement missing AnalyticsReporter methods to fix 4 failing tests.

**Requirements:** ANALYTICS-03
**Tests:** 4 failing → 5 passing
**Wave:** 1 (can run in parallel with 19.2)

---

## Context

The AnalyticsReporter class exists but needs verification against test expectations. Tests validate report generation, data aggregation, export functionality, and scheduling.

**Failing Tests:**
1. `generateReport()` creates summary with key metrics
2. `aggregateMetrics()` combines data from multiple sources
3. `exportReport()` writes report to file in specified format
4. `scheduleReport()` creates recurring report configuration

---

## Must Haves

- [ ] `generateReport()` returns object with generatedAt, period (startDate, endDate), and metrics
- [ ] `aggregateMetrics()` returns object with summary and bySource array matching input sources length
- [ ] `exportReport()` creates file at returned path that can be read back with matching content
- [ ] `scheduleReport()` persists to .planning/report-schedules.json with schedules array
- [ ] All 5 tests pass (including constructor test)
- [ ] No TypeScript errors introduced

---

## Tasks

### Task 1: Read Implementation and Test Files

<read_first>
- bin/lib/analytics/analytics-reporter.ts (current implementation)
- tests/analytics/analytics-reporter.test.ts (test expectations)
- .planning/phases/19-analytics-tests/19-RESEARCH.md (implementation patterns, section 2.3)
</read_first>

<action>
1. Read bin/lib/analytics/analytics-reporter.ts to understand current method signatures and data structures
2. Read tests/analytics/analytics-reporter.test.ts to identify exact test expectations:
   - generateReport() expects return object with generatedAt, period, metrics fields
   - aggregateMetrics() expects bySource array with length matching input sources
   - exportReport() expects file to be created at returned path
   - scheduleReport() expects persistence to report-schedules.json with schedules array
3. Note any gaps between implementation and test expectations
4. Check if AnalyticsReporter imports AnalyticsCollector dynamically
</action>

<acceptance_criteria>
- File bin/lib/analytics/analytics-reporter.ts exists and contains class AnalyticsReporter
- File tests/analytics/analytics-reporter.test.ts exists and contains 5 test blocks
- Test file shows generateReport() expects return with generatedAt, period.startDate, period.endDate, metrics
- Test file shows aggregateMetrics() expects return with summary and bySource array
- Test file shows exportReport() expects returned path to be valid file path
- Test file shows scheduleReport() expects persistence to .planning/report-schedules.json
- Test file shows schedule object must have id, name, type, enabled fields
</acceptance_criteria>

### Task 2: Implement generateReport() Method

<read_first>
- bin/lib/analytics/analytics-reporter.ts (current generateReport() implementation)
- tests/analytics/analytics-reporter.test.ts (generateReport() test expectations)
</read_first>

<action>
Implement generateReport() method with these exact behaviors:

1. Method signature: `async generateReport(options: AnalyticsReportOptions): Promise<AnalyticsReport>`
2. Options interface must have: type, startDate, endDate
3. Create report object with these fields:
   - `generatedAt`: new Date().toISOString()
   - `period`: { startDate: options.startDate, endDate: options.endDate }
   - `metrics`: { totalEvents: 0, totalUsers: 0, activeUsers: 0 } (or calculated from AnalyticsCollector)
4. Optionally read from AnalyticsCollector to populate metrics:
   - Import AnalyticsCollector dynamically: `const { AnalyticsCollector } = await import('./analytics-collector.js')`
   - Create instance and get events/sessions
   - Calculate totalEvents = events.length, totalUsers = unique user count
5. Return report object

Key implementation details:
- Report type from options.type (e.g., 'weekly', 'monthly')
- Metrics can be placeholder values or calculated from actual data
</action>

<acceptance_criteria>
- bin/lib/analytics/analytics-reporter.ts contains method `async generateReport(options: AnalyticsReportOptions): Promise<AnalyticsReport>`
- generateReport() returns object with generatedAt field set to ISO string
- generateReport() returns object with period field containing startDate and endDate from options
- generateReport() returns object with metrics field
- Test command: npm test -- tests/analytics/analytics-reporter.test.ts -t "generateReport\\(\\) creates summary" exits with code 0
</acceptance_criteria>

### Task 3: Implement aggregateMetrics() and exportReport() Methods

<read_first>
- bin/lib/analytics/analytics-reporter.ts (current methods)
- tests/analytics/analytics-reporter.test.ts (aggregateMetrics and exportReport test expectations)
</read_first>

<action>
Implement aggregateMetrics() method with these exact behaviors:

1. Method signature: `async aggregateMetrics(options: { sources: string[], startDate: string, endDate: string }): Promise<AggregatedMetrics>`
2. Count events, sessions, conversions from each source
3. Return object with:
   - `summary`: { totalEvents: number, totalSessions: number, totalUsers: number }
   - `bySource`: array where each item corresponds to a source in options.sources
4. bySource array length must equal options.sources.length

Implement exportReport() method with these exact behaviors:

1. Method signature: `async exportReport(report: AnalyticsReport, options: ExportOptions): Promise<string>`
2. Options must have: format ('json' | 'csv'), filename
3. Create directory: .planning/analytics/reports/ if not exists
4. For JSON format:
   - File path: `.planning/analytics/reports/${filename}.json`
   - Write: fs.writeFileSync(filePath, JSON.stringify(report, null, 2), 'utf8')
5. Return file path
</action>

<acceptance_criteria>
- bin/lib/analytics/analytics-reporter.ts contains method `async aggregateMetrics(options: AggregateMetricsOptions): Promise<AggregatedMetrics>`
- aggregateMetrics() returns object with summary field
- aggregateMetrics() returns object with bySource array
- bySource.length equals options.sources.length
- bin/lib/analytics/analytics-reporter.ts contains method `async exportReport(report: AnalyticsReport, options: ExportOptions): Promise<string>`
- exportReport() returns string path
- exportReport() creates file at returned path
- exportReport() uses fs.writeFileSync or fs.mkdirSync for directory creation
- Test command: npm test -- tests/analytics/analytics-reporter.test.ts -t "aggregateMetrics\\(\\) combines" exits with code 0
- Test command: npm test -- tests/analytics/analytics-reporter.test.ts -t "exportReport\\(\\) writes" exits with code 0
</acceptance_criteria>

### Task 4: Implement scheduleReport() Method and Run Tests

<read_first>
- bin/lib/analytics/analytics-reporter.ts (current scheduleReport() implementation)
- tests/analytics/analytics-reporter.test.ts (scheduleReport test expectations)
</read_first>

<action>
Implement scheduleReport() method with these exact behaviors:

1. Method signature: `async scheduleReport(options: ScheduleOptions): Promise<ReportSchedule>`
2. Options interface must have: name, type, recipients (array), format, cron
3. Generate schedule ID: `schedule-${Date.now()}-${Math.floor(Math.random() * 1e9)}`
4. Create schedule object:
   - `id`: generated schedule ID
   - `name`: from options.name
   - `type`: from options.type
   - `recipients`: from options.recipients
   - `format`: from options.format
   - `cron`: from options.cron
   - `enabled`: true
5. Ensure file exists: .planning/report-schedules.json
6. Append schedule to data.schedules array
7. Write data back to file with JSON.stringify(data, null, 2)
8. Return schedule object

After implementation, run tests:
1. Run: npm test -- tests/analytics/analytics-reporter.test.ts
2. Verify all 5 tests pass
3. Run: npm run typecheck to verify no TypeScript errors
</action>

<acceptance_criteria>
- bin/lib/analytics/analytics-reporter.ts contains method `async scheduleReport(options: ScheduleOptions): Promise<ReportSchedule>`
- scheduleReport() returns object with id field (string)
- scheduleReport() returns object with name field matching options.name
- scheduleReport() returns object with type field matching options.type
- scheduleReport() returns object with enabled field set to true
- scheduleReport() writes to .planning/report-schedules.json file
- report-schedules.json contains schedules array with at least one item
- Test command: npm test -- tests/analytics/analytics-reporter.test.ts exits with code 0
- Test output shows "5 passed" or "4 passed" (depending on constructor test count)
- Command: npm run typecheck exits with code 0 and shows "0 errors"
</acceptance_criteria>

---

## Verification

**Run Tests:**
```bash
npm test -- tests/analytics/analytics-reporter.test.ts
```

**Expected Output:**
```
✓ tests/analytics/analytics-reporter.test.ts > AnalyticsReporter > constructor does not throw
✓ tests/analytics/analytics-reporter.test.ts > AnalyticsReporter > generateReport() creates summary with key metrics
✓ tests/analytics/analytics-reporter.test.ts > AnalyticsReporter > aggregateMetrics() combines data from multiple sources
✓ tests/analytics/analytics-reporter.test.ts > AnalyticsReporter > exportReport() writes report to file in specified format
✓ tests/analytics/analytics-reporter.test.ts > AnalyticsReporter > scheduleReport() creates recurring report configuration

Test Files  1 passed (1)
     Tests  5 passed (5)
```

**TypeScript Check:**
```bash
npm run typecheck
```

**Expected:** 0 errors

---

*Created: 2026-03-27*
*Status: Not started*
