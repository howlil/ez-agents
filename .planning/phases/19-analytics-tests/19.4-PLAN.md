---
ez_plan_version: 1.0
phase: 19
plan: 19.4
milestone: v8.0
wave: 2
depends_on:
  - 19.2
  - 19.3
files_modified:
  - bin/lib/analytics/cohort-analyzer.ts
  - tests/analytics/cohort-analyzer.test.ts
requirements:
  - ANALYTICS-04
autonomous: true
---

# Phase 19.4: Implement CohortAnalyzer Methods

**Goal:** Implement missing CohortAnalyzer methods to fix 5 failing tests.

**Requirements:** ANALYTICS-04
**Tests:** 5 failing → 6 passing
**Wave:** 2 (depends on AnalyticsCollector patterns from 19.2)

---

## Context

The CohortAnalyzer class exists but needs verification against test expectations. Tests validate cohort definition, user assignment, retention calculation, and comparative analysis.

**Failing Tests:**
1. `defineCohort()` creates cohort by signup period
2. `addUserToCohort()` assigns user based on signup date
3. `calculateRetention()` returns retention rate per period
4. `compareCohorts()` returns comparative retention metrics
5. `getCohortMetrics()` returns size, activity, and lifetime value

---

## Must Haves

- [ ] `defineCohort()` creates cohort in .planning/cohorts.json with name, startDate, endDate, criteria
- [ ] `addUserToCohort()` matches signup date to cohort period and adds userId to memberships array
- [ ] `calculateRetention()` returns object with periods array where each period has rate >= 0
- [ ] `compareCohorts()` returns object with cohorts array containing comparative metrics
- [ ] `getCohortMetrics()` returns object with cohort name and size >= 0
- [ ] `recordActivity()` helper method tracks user activity for retention calculation
- [ ] All 6 tests pass (including constructor test)
- [ ] No TypeScript errors introduced

---

## Tasks

### Task 1: Read Implementation and Test Files

<read_first>
- bin/lib/analytics/cohort-analyzer.ts (current implementation)
- tests/analytics/cohort-analyzer.test.ts (test expectations)
- .planning/phases/19-analytics-tests/19-RESEARCH.md (implementation patterns, section 2.4)
</read_first>

<action>
1. Read bin/lib/analytics/cohort-analyzer.ts to understand current method signatures and data structures
2. Read tests/analytics/cohort-analyzer.test.ts to identify exact test expectations:
   - defineCohort() expects cohorts.json with cohorts array containing name, startDate, endDate
   - addUserToCohort() expects memberships object keyed by cohort name with userId array
   - calculateRetention() expects periods array with rate >= 0 for each period
   - compareCohorts() expects cohorts array with comparative metrics
   - getCohortMetrics() expects cohort name and size >= 0
3. Note the recordActivity() helper method used for retention tracking
4. Note test data patterns: 10 users in cohort, activity decreases over weeks
</action>

<acceptance_criteria>
- File bin/lib/analytics/cohort-analyzer.ts exists and contains class CohortAnalyzer
- File tests/analytics/cohort-analyzer.test.ts exists and contains 6 test blocks
- Test file shows defineCohort() expects cohorts.json with cohorts array
- Test file shows addUserToCohort() expects memberships object with cohort name keys
- Test file shows calculateRetention() expects periods array with rate values
- Test file shows compareCohorts() expects return with cohorts array
- Test file shows recordActivity() is called to track user activity per week
- Test file shows test data: 10 users, activity 10→7→5→3 over 4 weeks
</acceptance_criteria>

### Task 2: Implement defineCohort() and addUserToCohort() Methods

<read_first>
- bin/lib/analytics/cohort-analyzer.ts (current cohort methods)
- tests/analytics/cohort-analyzer.test.ts (cohort definition and user assignment tests)
</read_first>

<action>
Implement defineCohort() method with these exact behaviors:

1. Method signature: `async defineCohort(cohort: CohortDefinition): Promise<void>`
2. CohortDefinition interface must have: name, startDate, endDate, criteria
3. Create cohort object:
   - `name`: from cohort.name
   - `startDate`: from cohort.startDate
   - `endDate`: from cohort.endDate
   - `criteria`: from cohort.criteria
   - `size`: 0 (updated when users are added)
4. Ensure file exists: .planning/cohorts.json
5. Append cohort to data.cohorts array
6. Initialize data.memberships[cohort.name] = [] if not exists
7. Write data back to file with JSON.stringify(data, null, 2)

Implement addUserToCohort() method with these exact behaviors:

1. Method signature: `async addUserToCohort(userId: string, signupDate: string): Promise<void>`
2. Read cohorts.json file
3. Find cohort where signupDate is within [startDate, endDate] (string comparison works for ISO dates)
4. If cohort found:
   - Add userId to data.memberships[cohort.name] array
   - Increment cohort.size
5. Write data back to file
</action>

<acceptance_criteria>
- bin/lib/analytics/cohort-analyzer.ts contains method `async defineCohort(cohort: CohortDefinition): Promise<void>`
- defineCohort() writes to .planning/cohorts.json file
- cohorts.json contains cohorts array with cohort object having name, startDate, endDate fields
- bin/lib/analytics/cohort-analyzer.ts contains method `async addUserToCohort(userId: string, signupDate: string): Promise<void>`
- addUserToCohort() adds userId to data.memberships[cohortName] array
- addUserToCohort() matches signupDate to cohort where startDate <= signupDate <= endDate
- Test command: npm test -- tests/analytics/cohort-analyzer.test.ts -t "defineCohort\\(\\) creates cohort" exits with code 0
- Test command: npm test -- tests/analytics/cohort-analyzer.test.ts -t "addUserToCohort\\(\\) assigns user" exits with code 0
</acceptance_criteria>

### Task 3: Implement calculateRetention() and recordActivity() Methods

<read_first>
- bin/lib/analytics/cohort-analyzer.ts (current retention methods)
- tests/analytics/cohort-analyzer.test.ts (retention calculation tests)
</read_first>

<action>
Implement recordActivity() helper method with these exact behaviors:

1. Method signature: `async recordActivity(userId: string, timestamp: string): Promise<void>`
2. Read cohorts.json file
3. Append activity record to data.activities[userId] array (create if not exists)
4. Activity record: { userId, timestamp }
5. Write data back to file

Implement calculateRetention() method with these exact behaviors:

1. Method signature: `calculateRetention(cohortName: string, options?: { period?: string }): RetentionResult`
2. Find cohort by name in data.cohorts
3. Get cohort members from data.memberships[cohortName]
4. Calculate retention for 4 weeks (default period):
   - Week 0: initial cohort size
   - Week 1-3: count users with activity in that week
5. For each week, calculate rate: (activeUsers / initialSize) * 100
6. Return object with:
   - `cohort`: cohort name
   - `periods`: array of { period: string, rate: number }
   - `initialSize`: cohort size
</action>

<acceptance_criteria>
- bin/lib/analytics/cohort-analyzer.ts contains method `async recordActivity(userId: string, timestamp: string): Promise<void>`
- recordActivity() writes to .planning/cohorts.json file
- cohorts.json contains activities object with userId keys and activity arrays
- bin/lib/analytics/cohort-analyzer.ts contains method `calculateRetention(cohortName: string, options?: { period?: string }): RetentionResult`
- calculateRetention() returns object with periods array
- Each period in periods array has rate field with value >= 0
- calculateRetention() calculates rate as (activeUsers / initialSize) * 100
- Test command: npm test -- tests/analytics/cohort-analyzer.test.ts -t "calculateRetention\\(\\) returns retention" exits with code 0
</acceptance_criteria>

### Task 4: Implement compareCohorts(), getCohortMetrics(), and Run Tests

<read_first>
- bin/lib/analytics/cohort-analyzer.ts (current comparison and metrics methods)
- tests/analytics/cohort-analyzer.test.ts (comparison and metrics tests)
</read_first>

<action>
Implement compareCohorts() method with these exact behaviors:

1. Method signature: `async compareCohorts(cohortNames: string[]): Promise<{ cohorts: Array<{ name, size, retention }> }>`
2. For each cohort name in cohortNames:
   - Find cohort in data.cohorts
   - Get size from cohort.size or memberships length
   - Calculate retention rate using calculateRetention()
3. Return object with:
   - `cohorts`: array of { name, size, retention } for each cohort
   - Optionally include `summary` field

Implement getCohortMetrics() method with these exact behaviors:

1. Method signature: `async getCohortMetrics(cohortName: string): Promise<{ cohort, size, activity, lifetimeValue }>`
2. Find cohort by name
3. Get size from cohort.size or memberships length
4. Calculate activity rate (users with recent activity / total users)
5. Return object with:
   - `cohort` or `name`: cohort name
   - `size`: number >= 0
   - `activity`: activity rate (optional)
   - `lifetimeValue`: placeholder value (optional)

After implementation, run tests:
1. Run: npm test -- tests/analytics/cohort-analyzer.test.ts
2. Verify all 6 tests pass
3. Run: npm run typecheck to verify no TypeScript errors
</action>

<acceptance_criteria>
- bin/lib/analytics/cohort-analyzer.ts contains method `async compareCohorts(cohortNames: string[]): Promise<...>`
- compareCohorts() returns object with cohorts array
- cohorts array length is >= 1 when called with non-empty input
- bin/lib/analytics/cohort-analyzer.ts contains method `async getCohortMetrics(cohortName: string): Promise<...>`
- getCohortMetrics() returns object with cohort or name field
- getCohortMetrics() returns object with size field >= 0
- Test command: npm test -- tests/analytics/cohort-analyzer.test.ts -t "compareCohorts\\(\\) returns comparative" exits with code 0
- Test command: npm test -- tests/analytics/cohort-analyzer.test.ts -t "getCohortMetrics\\(\\) returns size" exits with code 0
- Test command: npm test -- tests/analytics/cohort-analyzer.test.ts exits with code 0
- Test output shows "6 passed" or "5 passed" (depending on constructor test count)
- Command: npm run typecheck exits with code 0 and shows "0 errors"
</acceptance_criteria>

---

## Verification

**Run Tests:**
```bash
npm test -- tests/analytics/cohort-analyzer.test.ts
```

**Expected Output:**
```
✓ tests/analytics/cohort-analyzer.test.ts > CohortAnalyzer > constructor does not throw
✓ tests/analytics/cohort-analyzer.test.ts > CohortAnalyzer > defineCohort() creates cohort by signup period
✓ tests/analytics/cohort-analyzer.test.ts > CohortAnalyzer > addUserToCohort() assigns user based on signup date
✓ tests/analytics/cohort-analyzer.test.ts > CohortAnalyzer > calculateRetention() returns retention rate per period
✓ tests/analytics/cohort-analyzer.test.ts > CohortAnalyzer > compareCohorts() returns comparative retention metrics
✓ tests/analytics/cohort-analyzer.test.ts > CohortAnalyzer > getCohortMetrics() returns size, activity, and lifetime value

Test Files  1 passed (1)
     Tests  6 passed (6)
```

**TypeScript Check:**
```bash
npm run typecheck
```

**Expected:** 0 errors

---

*Created: 2026-03-27*
*Status: Not started*
