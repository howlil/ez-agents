---
ez_plan_version: 1.0
phase: 19
plan: 19.5
milestone: v8.0
wave: 2
depends_on:
  - 19.2
  - 19.3
files_modified:
  - bin/lib/analytics/funnel-analyzer.ts
  - tests/analytics/funnel-analyzer.test.ts
requirements:
  - ANALYTICS-05
autonomous: true
---

# Phase 19.5: Implement FunnelAnalyzer Methods

**Goal:** Implement missing FunnelAnalyzer methods to fix 5 failing tests.

**Requirements:** ANALYTICS-05
**Tests:** 5 failing → 6 passing
**Wave:** 2 (depends on AnalyticsCollector patterns from 19.2)

---

## Context

The FunnelAnalyzer class exists but needs verification against test expectations. Tests validate funnel definition, conversion tracking, rate calculation, and drop-off analysis.

**Failing Tests:**
1. `defineFunnel()` creates funnel with ordered steps
2. `trackConversion()` records user progression through funnel
3. `getConversionRates()` returns percentage at each step
4. `getDropOffPoints()` identifies biggest conversion losses
5. `compareFunnels()` returns comparative metrics between funnels

---

## Must Haves

- [ ] `defineFunnel()` creates funnel in .planning/funnels.json with name and steps sorted by order
- [ ] `trackConversion()` appends conversion record to conversions object keyed by funnel name
- [ ] `getConversionRates()` returns object with steps array where each step has users >= 0
- [ ] `getDropOffPoints()` returns object with points array (can be empty) and totalUsers >= 0
- [ ] `compareFunnels()` returns object keyed by funnel name with metrics per funnel
- [ ] All 6 tests pass (including constructor test)
- [ ] No TypeScript errors introduced

---

## Tasks

### Task 1: Read Implementation and Test Files

<read_first>
- bin/lib/analytics/funnel-analyzer.ts (current implementation)
- tests/analytics/funnel-analyzer.test.ts (test expectations)
- .planning/phases/19-analytics-tests/19-RESEARCH.md (implementation patterns, section 2.5)
</read_first>

<action>
1. Read bin/lib/analytics/funnel-analyzer.ts to understand current method signatures and data structures
2. Read tests/analytics/funnel-analyzer.test.ts to identify exact test expectations:
   - defineFunnel() expects funnels.json with funnels array, steps sorted by order
   - trackConversion() expects conversions object keyed by funnel name with array of conversions
   - getConversionRates() expects steps array with users count per step
   - getDropOffPoints() expects points array and totalUsers
   - compareFunnels() expects object keyed by funnel name
3. Note test data patterns: 10 users view, 5 click, 2 submit for conversion rates
4. Note test data patterns: 100 view, 30 click for drop-off analysis
</action>

<acceptance_criteria>
- File bin/lib/analytics/funnel-analyzer.ts exists and contains class FunnelAnalyzer
- File tests/analytics/funnel-analyzer.test.ts exists and contains 6 test blocks
- Test file shows defineFunnel() expects steps sorted by order field
- Test file shows trackConversion() expects conversions object with funnel name keys
- Test file shows getConversionRates() expects steps array with users field
- Test file shows getDropOffPoints() expects points array (can be empty)
- Test file shows compareFunnels() expects return object keyed by funnel name
- Test file shows test data: 10 users view, 5 click, 2 submit for conversion rates
</acceptance_criteria>

### Task 2: Implement defineFunnel() Method

<read_first>
- bin/lib/analytics/funnel-analyzer.ts (current defineFunnel() implementation)
- tests/analytics/funnel-analyzer.test.ts (defineFunnel test expectations)
</read_first>

<action>
Implement defineFunnel() method with these exact behaviors:

1. Method signature: `async defineFunnel(funnel: FunnelDefinition): Promise<void>`
2. FunnelDefinition interface must have: name, steps (array of { name, order })
3. Sort steps by order field: funnel.steps.sort((a, b) => a.order - b.order)
4. Create funnel object:
   - `name`: from funnel.name
   - `steps`: sorted steps array
   - `createdAt`: new Date().toISOString()
5. Ensure file exists: .planning/funnels.json
6. Append funnel to data.funnels array
7. Initialize data.conversions[funnel.name] = [] if not exists
8. Write data back to file with JSON.stringify(data, null, 2)
</action>

<acceptance_criteria>
- bin/lib/analytics/funnel-analyzer.ts contains method `async defineFunnel(funnel: FunnelDefinition): Promise<void>`
- defineFunnel() sorts steps by order field using steps.sort((a, b) => a.order - b.order)
- defineFunnel() writes to .planning/funnels.json file
- funnels.json contains funnels array with funnel object having name and steps fields
- steps[0].name is the step with lowest order value
- steps[last].name is the step with highest order value
- Test command: npm test -- tests/analytics/funnel-analyzer.test.ts -t "defineFunnel\\(\\) creates funnel" exits with code 0
</acceptance_criteria>

### Task 3: Implement trackConversion() Method

<read_first>
- bin/lib/analytics/funnel-analyzer.ts (current trackConversion() implementation)
- tests/analytics/funnel-analyzer.test.ts (trackConversion test expectations)
</read_first>

<action>
Implement trackConversion() method with these exact behaviors:

1. Method signature: `async trackConversion(funnelName: string, userId: string, completedSteps: string[]): Promise<void>`
2. Read funnels.json file
3. Create conversion record:
   - `userId`: from userId parameter
   - `steps`: from completedSteps array
   - `timestamp`: new Date().toISOString()
4. Append conversion record to data.conversions[funnelName] array
5. If data.conversions[funnelName] doesn't exist, initialize as empty array first
6. Write data back to file with JSON.stringify(data, null, 2)
</action>

<acceptance_criteria>
- bin/lib/analytics/funnel-analyzer.ts contains method `async trackConversion(funnelName: string, userId: string, completedSteps: string[]): Promise<void>`
- trackConversion() writes to .planning/funnels.json file
- funnels.json contains conversions object with funnel name keys
- conversions[funnelName] is an array of conversion records
- Each conversion record has userId, steps, and timestamp fields
- Test command: npm test -- tests/analytics/funnel-analyzer.test.ts -t "trackConversion\\(\\) records user" exits with code 0
</acceptance_criteria>

### Task 4: Implement getConversionRates() Method

<read_first>
- bin/lib/analytics/funnel-analyzer.ts (current getConversionRates() implementation)
- tests/analytics/funnel-analyzer.test.ts (getConversionRates test expectations)
</read_first>

<action>
Implement getConversionRates() method with these exact behaviors:

1. Method signature: `async getConversionRates(funnelName: string): Promise<{ funnel, steps: [{ name, users, rate }] }>`
2. Find funnel by name in data.funnels
3. Get conversions from data.conversions[funnelName]
4. For each step in funnel.steps:
   - Count unique users who completed that step: users who have step name in their completedSteps array
   - Calculate rate: (usersAtStep / totalUsers) * 100
5. Return object with:
   - `funnel`: funnel name
   - `steps`: array of { name, users, rate } for each step
</action>

<acceptance_criteria>
- bin/lib/analytics/funnel-analyzer.ts contains method `async getConversionRates(funnelName: string): Promise<...>`
- getConversionRates() returns object with steps array
- Each step in steps array has name, users, and rate fields
- users field is count of unique users who completed that step
- rate field is percentage (users / totalUsers * 100)
- Test command: npm test -- tests/analytics/funnel-analyzer.test.ts -t "getConversionRates\\(\\) returns percentage" exits with code 0
</acceptance_criteria>

### Task 5: Implement getDropOffPoints() and compareFunnels(), Run Tests

<read_first>
- bin/lib/analytics/funnel-analyzer.ts (current drop-off and comparison methods)
- tests/analytics/funnel-analyzer.test.ts (getDropOffPoints and compareFunnels tests)
</read_first>

<action>
Implement getDropOffPoints() method with these exact behaviors:

1. Method signature: `async getDropOffPoints(funnelName: string): Promise<{ points, totalUsers }>`
2. Find funnel by name in data.funnels
3. Get conversions from data.conversions[funnelName]
4. Count users at each step
5. Compare consecutive steps to find drop-off:
   - dropRate = (usersAtStep1 - usersAtStep2) / usersAtStep1 * 100
6. Return object with:
   - `points`: array of { fromStep, toStep, dropRate } (can be empty if no drop-off data)
   - `totalUsers`: total unique users in funnel

Implement compareFunnels() method with these exact behaviors:

1. Method signature: `async compareFunnels(funnelNames: string[]): Promise<Record<string, { totalUsers, conversionRate, steps }>>`
2. For each funnel name in funnelNames:
   - Get total users from conversions
   - Calculate conversion rate (completed all steps / total users * 100)
   - Get step metrics
3. Return object keyed by funnel name with metrics

After implementation, run tests:
1. Run: npm test -- tests/analytics/funnel-analyzer.test.ts
2. Verify all 6 tests pass
3. Run: npm run typecheck to verify no TypeScript errors
</action>

<acceptance_criteria>
- bin/lib/analytics/funnel-analyzer.ts contains method `async getDropOffPoints(funnelName: string): Promise<...>`
- getDropOffPoints() returns object with totalUsers field >= 0
- getDropOffPoints() returns object with points array (can be empty)
- bin/lib/analytics/funnel-analyzer.ts contains method `async compareFunnels(funnelNames: string[]): Promise<...>`
- compareFunnels() returns object with keys matching input funnel names
- Each funnel metric has totalUsers and conversionRate fields
- Test command: npm test -- tests/analytics/funnel-analyzer.test.ts -t "getDropOffPoints\\(\\) identifies" exits with code 0
- Test command: npm test -- tests/analytics/funnel-analyzer.test.ts -t "compareFunnels\\(\\) returns comparative" exits with code 0
- Test command: npm test -- tests/analytics/funnel-analyzer.test.ts exits with code 0
- Test output shows "6 passed" or "5 passed" (depending on constructor test count)
- Command: npm run typecheck exits with code 0 and shows "0 errors"
</acceptance_criteria>

---

## Verification

**Run Tests:**
```bash
npm test -- tests/analytics/funnel-analyzer.test.ts
```

**Expected Output:**
```
✓ tests/analytics/funnel-analyzer.test.ts > FunnelAnalyzer > constructor does not throw
✓ tests/analytics/funnel-analyzer.test.ts > FunnelAnalyzer > defineFunnel() creates funnel with ordered steps
✓ tests/analytics/funnel-analyzer.test.ts > FunnelAnalyzer > trackConversion() records user progression through funnel
✓ tests/analytics/funnel-analyzer.test.ts > FunnelAnalyzer > getConversionRates() returns percentage at each step
✓ tests/analytics/funnel-analyzer.test.ts > FunnelAnalyzer > getDropOffPoints() identifies biggest conversion losses
✓ tests/analytics/funnel-analyzer.test.ts > FunnelAnalyzer > compareFunnels() returns comparative metrics between funnels

Test Files  1 passed (1)
     Tests  6 passed (6)
```

**TypeScript Check:**
```bash
npm run typecheck
```

**Expected:** 0 errors

---

*Created: 2026-03-27*
*Status: Not started*
