---
wave: 10.6
depends_on:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/10-foundation-core-library-refactoring/10-CONTEXT.md
  - .planning/phases/10-foundation-core-library-refactoring/10-RESEARCH.md
  - .planning/phases/10-foundation-core-library-refactoring/10-PLAN-01.md
  - .planning/phases/10-foundation-core-library-refactoring/10-PLAN-02.md
files_modified:
  - bin/lib/factories/
  - bin/lib/agent-registry.ts (or agent creation code)
  - bin/lib/index.ts
autonomous: true
---

# Plan 10.6: Apply Factory Pattern for Object Creation (CORE-02)

**Requirement:** CORE-02 — Apply Factory pattern for object creation in module instantiation

**Goal:** Implement Factory pattern with registry for agent instantiation to enable runtime extensibility and decouple agent consumers from concrete implementations.

---

## Tasks

### Task 1: Define Agent Interface and Factory Types

<read_first>
- .planning/phases/10-foundation-core-library-refactoring/10-RESEARCH.md (Factory Pattern section)
- bin/lib/ (search for existing agent definitions or IAgent interface)
</read_first>

<action>
Create agent interface and factory types at `bin/lib/factories/types.ts`:

1. Export `IAgent` interface:
   ```typescript
   export interface IAgent {
     getName(): string;
     getDescription(): string;
     execute(context: AgentContext): Promise<AgentResult>;
     validate(): Promise<boolean>;
   }
   ```

2. Export `AgentContext` interface:
   ```typescript
   export interface AgentContext {
     task: string;
     files?: string[];
     variables?: Record<string, string>;
     phase?: string;
   }
   ```

3. Export `AgentResult` interface:
   ```typescript
   export interface AgentResult {
     success: boolean;
     output?: string;
     error?: string;
     nextAgent?: string;
   }
   ```

4. Export `AgentConfig` interface:
   ```typescript
   export interface AgentConfig {
     name: string;
     modelProfile?: string;
     variables?: Record<string, string>;
     [key: string]: any;
   }
   ```

5. Export `AgentFactory` type:
   ```typescript
   export type AgentFactory = (config: AgentConfig) => IAgent;
   ```
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/factories/types.ts`
- [ ] Grep pattern `export interface IAgent` matches
- [ ] Grep pattern `execute\(context: AgentContext\)` matches in IAgent
- [ ] Grep pattern `export interface AgentContext` matches
- [ ] Grep pattern `export interface AgentResult` matches
- [ ] Grep pattern `export interface AgentConfig` matches
- [ ] Grep pattern `export type AgentFactory` matches
</acceptance_criteria>

---

### Task 2: Implement AgentFactory Class

<read_first>
- bin/lib/factories/types.ts (after Task 1)
- bin/lib/ (search for existing agent creation code)
</read_first>

<action>
Implement `bin/lib/factories/AgentFactory.ts`:

1. Import types from './types.js'
2. Import `defaultLogger as logger` from '../logger.js'
3. Create `AgentFactoryRegistry` class:
   - Private static instance for singleton: `private static instance: AgentFactoryRegistry | null`
   - Private constructor
   - Private field `factories: Map<string, AgentFactory>`
   - Static `getInstance(): AgentFactoryRegistry` method

4. Implement factory methods:
   - `registerAgent(type: string, factory: AgentFactory): void` — Register agent factory
   - `createAgent(type: string, config: AgentConfig): IAgent` — Create agent instance
   - `hasAgent(type: string): boolean` — Check if agent type is registered
   - `getRegisteredTypes(): string[]` — Get list of registered agent types
   - `unregisterAgent(type: string): void` — Remove agent factory

5. Apply `@LogExecution` decorator to all public methods (from Plan 10.2)
6. Log agent creation at info level
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/factories/AgentFactory.ts`
- [ ] Grep pattern `export class AgentFactoryRegistry` matches
- [ ] Grep pattern `private static instance: AgentFactoryRegistry` matches (singleton)
- [ ] Grep pattern `getInstance\(\): AgentFactoryRegistry` matches
- [ ] Grep pattern `registerAgent\(type: string` matches
- [ ] Grep pattern `createAgent\(type: string` matches
- [ ] Grep pattern `hasAgent\(type: string\): boolean` matches
- [ ] Grep pattern `getRegisteredTypes\(\): string\[\]` matches
- [ ] Grep pattern `factories: Map<string, AgentFactory>` matches
- [ ] Grep pattern `@LogExecution` matches on public methods
</acceptance_criteria>

---

### Task 3: Create EzPlannerAgent

<read_first>
- bin/lib/factories/types.ts (after Task 1)
- .planning/ (search for ez-planner agent definition)
- workflow-templates/ (search for planner template)
</read_first>

<action>
Implement `bin/lib/factories/EzPlannerAgent.ts`:

1. Import `IAgent`, `AgentContext`, `AgentResult` from './types.js'
2. Import `defaultLogger as logger` from '../logger.js'
3. Create `EzPlannerAgent` class implementing `IAgent`:
   - Private field `name: string`
   - Private field `config: AgentConfig`
   - Constructor accepting AgentConfig
   - `getName(): string` — returns 'ez-planner'
   - `getDescription(): string` — returns planner description
   - `execute(context: AgentContext): Promise<AgentResult>` — Execute planner logic
   - `validate(): Promise<boolean>` — Validate agent configuration
4. Apply `@LogExecution` decorator to execute and validate methods
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/factories/EzPlannerAgent.ts`
- [ ] Grep pattern `export class EzPlannerAgent` matches
- [ ] Grep pattern `implements IAgent` matches
- [ ] Grep pattern `getName\(\): string` matches
- [ ] Grep pattern `async execute\(context: AgentContext\)` matches
- [ ] Grep pattern `async validate\(\): Promise<boolean>` matches
- [ ] Grep pattern `returns 'ez-planner'` or `return 'ez-planner'` matches
- [ ] Grep pattern `@LogExecution` matches
</acceptance_criteria>

---

### Task 4: Create EzRoadmapperAgent

<read_first>
- bin/lib/factories/types.ts
- .planning/ (search for ez-roadmapper agent definition)
</read_first>

<action>
Implement `bin/lib/factories/EzRoadmapperAgent.ts`:

1. Import `IAgent`, `AgentContext`, `AgentResult` from './types.js'
2. Import `defaultLogger as logger` from '../logger.js'
3. Create `EzRoadmapperAgent` class implementing `IAgent`:
   - Private field `name: string`
   - Private field `config: AgentConfig`
   - Constructor accepting AgentConfig
   - `getName(): string` — returns 'ez-roadmapper'
   - `getDescription(): string` — returns roadmapper description
   - `execute(context: AgentContext): Promise<AgentResult>` — Execute roadmapper logic
   - `validate(): Promise<boolean>` — Validate agent configuration
4. Apply `@LogExecution` decorator to execute and validate methods
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/factories/EzRoadmapperAgent.ts`
- [ ] Grep pattern `export class EzRoadmapperAgent` matches
- [ ] Grep pattern `implements IAgent` matches
- [ ] Grep pattern `getName\(\): string` matches
- [ ] Grep pattern `async execute\(context: AgentContext\)` matches
- [ ] Grep pattern `returns 'ez-roadmapper'` or `return 'ez-roadmapper'` matches
- [ ] Grep pattern `@LogExecution` matches
</acceptance_criteria>

---

### Task 5: Create EzExecutorAgent

<read_first>
- bin/lib/factories/types.ts
- .planning/ (search for ez-executor agent definition)
</read_first>

<action>
Implement `bin/lib/factories/EzExecutorAgent.ts`:

1. Import `IAgent`, `AgentContext`, `AgentResult` from './types.js'
2. Import `defaultLogger as logger` from '../logger.js'
3. Create `EzExecutorAgent` class implementing `IAgent`:
   - Private field `name: string`
   - Private field `config: AgentConfig`
   - Constructor accepting AgentConfig
   - `getName(): string` — returns 'ez-executor'
   - `getDescription(): string` — returns executor description
   - `execute(context: AgentContext): Promise<AgentResult>` — Execute executor logic
   - `validate(): Promise<boolean>` — Validate agent configuration
4. Apply `@LogExecution` decorator to execute and validate methods
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/factories/EzExecutorAgent.ts`
- [ ] Grep pattern `export class EzExecutorAgent` matches
- [ ] Grep pattern `implements IAgent` matches
- [ ] Grep pattern `getName\(\): string` matches
- [ ] Grep pattern `async execute\(context: AgentContext\)` matches
- [ ] Grep pattern `returns 'ez-executor'` or `return 'ez-executor'` matches
- [ ] Grep pattern `@LogExecution` matches
</acceptance_criteria>

---

### Task 6: Register Default Agents

<read_first>
- bin/lib/factories/AgentFactory.ts (after Task 2)
- bin/lib/factories/EzPlannerAgent.ts (after Task 3)
- bin/lib/factories/EzRoadmapperAgent.ts (after Task 4)
- bin/lib/factories/EzExecutorAgent.ts (after Task 5)
</read_first>

<action>
Create `bin/lib/factories/registerAgents.ts` to register default agents:

1. Import `AgentFactoryRegistry` from './AgentFactory.js'
2. Import agent classes
3. Export `registerDefaultAgents(): void` function:
   ```typescript
   export function registerDefaultAgents(): void {
     const registry = AgentFactoryRegistry.getInstance();
     
     registry.registerAgent('ez-planner', (config) => new EzPlannerAgent(config));
     registry.registerAgent('ez-roadmapper', (config) => new EzRoadmapperAgent(config));
     registry.registerAgent('ez-executor', (config) => new EzExecutorAgent(config));
   }
   ```

4. Export `getRegisteredAgents(): string[]` function
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/factories/registerAgents.ts`
- [ ] Grep pattern `export function registerDefaultAgents` matches
- [ ] Grep pattern `export function getRegisteredAgents` matches
- [ ] Grep pattern `registry.registerAgent\('ez-planner'` matches
- [ ] Grep pattern `registry.registerAgent\('ez-roadmapper'` matches
- [ ] Grep pattern `registry.registerAgent\('ez-executor'` matches
</acceptance_criteria>

---

### Task 7: Create Factory Barrel Export

<read_first>
- bin/lib/factories/types.ts (after Task 1)
- bin/lib/factories/AgentFactory.ts (after Task 2)
- bin/lib/factories/EzPlannerAgent.ts (after Task 3)
- bin/lib/factories/EzRoadmapperAgent.ts (after Task 4)
- bin/lib/factories/EzExecutorAgent.ts (after Task 5)
- bin/lib/factories/registerAgents.ts (after Task 6)
</read_first>

<action>
Create `bin/lib/factories/index.ts` with barrel exports:

1. Export types:
   - `export type { IAgent, AgentConfig, AgentContext, AgentResult, AgentFactory } from './types.js'`

2. Export factory:
   - `export { AgentFactoryRegistry } from './AgentFactory.js'`

3. Export agent classes:
   - `export { EzPlannerAgent } from './EzPlannerAgent.js'`
   - `export { EzRoadmapperAgent } from './EzRoadmapperAgent.js'`
   - `export { EzExecutorAgent } from './EzExecutorAgent.js'`

4. Export registration:
   - `export { registerDefaultAgents, getRegisteredAgents } from './registerAgents.js'`
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/factories/index.ts`
- [ ] Grep pattern `export type { IAgent` matches
- [ ] Grep pattern `export { AgentFactoryRegistry }` matches
- [ ] Grep pattern `export { EzPlannerAgent }` matches
- [ ] Grep pattern `export { EzRoadmapperAgent }` matches
- [ ] Grep pattern `export { EzExecutorAgent }` matches
- [ ] Grep pattern `export { registerDefaultAgents` matches
</acceptance_criteria>

---

### Task 8: Update Main Barrel Export

<read_first>
- bin/lib/index.ts (current barrel export)
- bin/lib/factories/index.ts (after Task 7)
</read_first>

<action>
Update `bin/lib/index.ts` to re-export Factory pattern:

1. Add factory exports:
   ```typescript
   export type { IAgent, AgentConfig, AgentContext, AgentResult, AgentFactory } from './factories/index.js'
   export { AgentFactoryRegistry } from './factories/index.js'
   export { EzPlannerAgent, EzRoadmapperAgent, EzExecutorAgent } from './factories/index.js'
   export { registerDefaultAgents, getRegisteredAgents } from './factories/index.js'
   ```

2. Verify exports don't conflict with existing exports
</action>

<acceptance_criteria>
- [ ] File `bin/lib/index.ts` exports `IAgent` type
- [ ] File exports `AgentConfig` type
- [ ] File exports `AgentContext` type
- [ ] File exports `AgentResult` type
- [ ] File exports `AgentFactory` type
- [ ] File exports `AgentFactoryRegistry`
- [ ] File exports `EzPlannerAgent`
- [ ] File exports `EzRoadmapperAgent`
- [ ] File exports `EzExecutorAgent`
- [ ] File exports `registerDefaultAgents`
- [ ] Grep pattern `export.*IAgent.*from.*factories` matches
</acceptance_criteria>

---

### Task 9: Run Tests and Verify

<read_first>
- vitest.config.ts (test configuration)
- package.json (test scripts)
</read_first>

<action>
Run full test suite to verify Factory pattern implementation doesn't break existing functionality:

1. Run tests: `npm test`
2. Check test coverage: `npm run test:coverage`
3. Fix any failing tests
4. Verify 70%+ coverage threshold maintained
5. Run lint: `npm run lint`
6. Fix any ESLint warnings
7. Verify build: `npm run build`
</action>

<acceptance_criteria>
- [ ] All tests pass: `npm test` exits with code 0
- [ ] Test coverage >= 70%: `npm run test:coverage` shows >= 70
- [ ] Zero ESLint warnings: `npm run lint` exits with code 0
- [ ] Build succeeds: `npm run build` exits with code 0
- [ ] No runtime errors in test output
- [ ] AgentFactoryRegistry is singleton (verify with grep: `private static instance`)
- [ ] All three agents created (EzPlannerAgent, EzRoadmapperAgent, EzExecutorAgent)
- [ ] Factory registry functional (verify with grep: `registerAgent`, `createAgent`)
</acceptance_criteria>

---

## Verification Criteria

**Goal-backward verification (must_haves):**

- [ ] **CORE-02 addressed:** Factory pattern implemented for agent creation
- [ ] **IAgent interface:** Defines contract for all agents
- [ ] **AgentFactoryRegistry:** Singleton registry for agent factories
- [ ] **Registry methods:** registerAgent(), createAgent(), hasAgent(), getRegisteredTypes()
- [ ] **Three agents implemented:** EzPlannerAgent, EzRoadmapperAgent, EzExecutorAgent
- [ ] **Agent registration:** registerDefaultAgents() function for default agent registration
- [ ] **Runtime extensibility:** New agent types can be registered dynamically
- [ ] **@LogExecution applied:** All factory methods and agents have logging decorator
- [ ] **Backward compatibility:** All existing exports maintained in bin/lib/index.ts
- [ ] **Tests passing:** All 472+ tests still passing
- [ ] **Coverage maintained:** 70%+ code coverage threshold
- [ ] **Build succeeds:** TypeScript compiles without errors
- [ ] **Zero ESLint warnings:** Code quality maintained

**Implementation order verification:**

- [ ] Task 1 (Types) completed before Task 2 (AgentFactoryRegistry)
- [ ] Task 2 (Factory) completed before Tasks 3-5 (agent implementations)
- [ ] Tasks 3-5 (Agents) completed in any order
- [ ] Task 6 (Registration) completed after Tasks 3-5
- [ ] Task 7 (Factory barrel) completed after Tasks 1-6
- [ ] Task 8 (Main barrel) completed after Task 7
- [ ] Task 9 (Tests) completed last as verification

---

## Notes

**Dependencies on other Phase 10 plans:**
- This plan (10.6) depends on Plan 10.1 (CORE-01) for class-based architecture
- This plan depends on Plan 10.2 (CORE-06) for @LogExecution decorator
- Plan 10.7 (Facade Pattern) will use AgentFactoryRegistry through facades

**Benefits:**
- Enables runtime extensibility (new agent types can be registered dynamically)
- Centralizes agent creation logic
- Decouples agent consumers from concrete implementations
- Easy to add new agent types without changing core code

**Risk mitigation:**
- All agents implement same interface (type safety)
- Factory validates agent type before creation
- Run full test suite after each agent implementation

---

*Plan created: 2026-03-25*
*Phase: 10 | Wave: 10.6 | Requirements: CORE-02*
