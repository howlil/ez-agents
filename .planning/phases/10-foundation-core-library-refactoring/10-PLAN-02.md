---
wave: 10.2
depends_on:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/10-foundation-core-library-refactoring/10-CONTEXT.md
  - .planning/phases/10-foundation-core-library-refactoring/10-RESEARCH.md
  - .planning/phases/10-foundation-core-library-refactoring/10-PLAN-01.md
files_modified:
  - bin/lib/decorators/
  - bin/lib/context-*.ts
  - bin/lib/skill-*.ts
  - bin/lib/session-manager.ts
  - bin/lib/index.ts
autonomous: true
---

# Plan 10.2: Apply Decorator Pattern for Cross-Cutting Concerns (CORE-06)

**Requirement:** CORE-06 — Apply Decorator pattern for cross-cutting concerns

**Goal:** Implement TypeScript decorators for logging, caching, and validation across all service classes to separate cross-cutting concerns from business logic.

---

## Tasks

### Task 1: Create Decorator Type Definitions

<read_first>
- .planning/phases/10-foundation-core-library-refactoring/10-RESEARCH.md (Decorator Implementation Details section)
- tsconfig.json (current TypeScript configuration)
- bin/lib/logger.ts (existing Logger class for decorator usage)
</read_first>

<action>
Create type definitions file at `bin/lib/decorators/types.ts` with:

1. **LogExecutionOptions** interface:
   - `logParams?: boolean` — Log method parameters
   - `logResult?: boolean` — Log method return value
   - `logDuration?: boolean` — Log execution duration
   - `level?: 'info' | 'debug' | 'trace' | 'warn' | 'error'` — Log level
   - `paramName?: string` — Specific parameter name to log

2. **CacheResultOptions** interface:
   - `ttl?: number` — Time to live in milliseconds (default: 300000)
   - `cacheKeyFn?: (...args: any[]) => string` — Custom cache key generator

3. **ValidateInputOptions** interface:
   - `validatorFn: (...args: any[]) => void` — Validation function that throws on failure

4. Export all interfaces with `export type` statements
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/decorators/types.ts`
- [ ] Grep pattern `export interface LogExecutionOptions` matches in types.ts
- [ ] Grep pattern `export interface CacheResultOptions` matches in types.ts
- [ ] Grep pattern `export interface ValidateInputOptions` matches in types.ts
- [ ] LogExecutionOptions contains properties: logParams, logResult, logDuration, level, paramName
- [ ] CacheResultOptions contains properties: ttl, cacheKeyFn
- [ ] ValidateInputOptions contains property: validatorFn
</acceptance_criteria>

---

### Task 2: Implement @LogExecution Decorator

<read_first>
- .planning/phases/10-foundation-core-library-refactoring/10-RESEARCH.md (LogExecution example)
- bin/lib/decorators/types.ts (after Task 1)
- bin/lib/logger.ts (Logger class usage)
</read_first>

<action>
Implement `bin/lib/decorators/LogExecution.ts` with:

1. Import `LogExecutionOptions` from types.ts
2. Import `defaultLogger as logger` from '../logger.js'
3. Export function `LogExecution(methodName: string, options: LogExecutionOptions = {})`
4. Decorator must:
   - Accept methodName string and optional options object
   - Return PropertyDecorator function
   - Wrap original method to log entry/exit
   - Handle both sync and async methods (Promise detection)
   - Log format: `[ClassName] Entering/Completed/Failed methodName`
   - Include duration in ms
   - Optionally log params and result based on options
   - Catch and log errors before re-throwing
5. Use logger with configurable log level (default: 'debug')
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/decorators/LogExecution.ts`
- [ ] Grep pattern `export function LogExecution` matches in LogExecution.ts
- [ ] Grep pattern `LogExecution\(methodName: string` matches
- [ ] Grep pattern `Entering.*methodName` matches (log entry message)
- [ ] Grep pattern `Completed.*methodName` matches (log exit message)
- [ ] Grep pattern `Failed.*methodName` matches (log error message)
- [ ] Grep pattern `Date.now\(\)` matches (duration calculation)
- [ ] Grep pattern `instanceof Promise` matches (async handling)
- [ ] Decorator imports logger from '../logger.js'
</acceptance_criteria>

---

### Task 3: Implement @CacheResult Decorator

<read_first>
- .planning/phases/10-foundation-core-library-refactoring/10-RESEARCH.md (CacheResult example)
- bin/lib/decorators/types.ts
- bin/lib/logger.ts
</read_first>

<action>
Implement `bin/lib/decorators/CacheResult.ts` with:

1. Import `CacheResultOptions` from types.ts
2. Import `defaultLogger as logger` from '../logger.js'
3. Export function `CacheResult(cacheKeyFn: (...args: any[]) => string, ttl: number = 300000)`
4. Decorator must:
   - Accept cacheKeyFn and optional ttl (default: 300000ms / 5 minutes)
   - Maintain internal `Map<string, { value: any; expiry: number }>` for cache storage
   - Check cache on method call: if key exists and not expired, return cached value
   - Execute original method if cache miss, cache result with expiry
   - Log cache hit/miss at debug level
   - Handle both sync and async methods
5. Return PropertyDecorator function
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/decorators/CacheResult.ts`
- [ ] Grep pattern `export function CacheResult` matches in CacheResult.ts
- [ ] Grep pattern `cacheKeyFn: \(\.\.\.args: any\[\]\) => string` matches
- [ ] Grep pattern `ttl: number = 300000` matches (default 5 minutes)
- [ ] Grep pattern `Map<string, { value: any; expiry: number }>` matches (cache storage)
- [ ] Grep pattern `Cache hit` matches (hit logging)
- [ ] Grep pattern `Cache miss` matches (miss logging)
- [ ] Grep pattern `expiry: number` matches (expiry tracking)
- [ ] Decorator imports logger from '../logger.js'
</acceptance_criteria>

---

### Task 4: Implement @ValidateInput Decorator

<read_first>
- .planning/phases/10-foundation-core-library-refactoring/10-RESEARCH.md (ValidateInput example)
- bin/lib/decorators/types.ts
</read_first>

<action>
Implement `bin/lib/decorators/ValidateInput.ts` with:

1. Import `ValidateInputOptions` from types.ts
2. Export function `ValidateInput(validatorFn: (...args: any[]) => void)`
3. Decorator must:
   - Accept validatorFn that throws on validation failure
   - Execute validator before original method
   - Pass through method result on success
   - Throw validation error if validator throws
4. Return PropertyDecorator function
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/decorators/ValidateInput.ts`
- [ ] Grep pattern `export function ValidateInput` matches in ValidateInput.ts
- [ ] Grep pattern `validatorFn: \(\.\.\.args: any\[\]\) => void` matches
- [ ] Grep pattern `validatorFn\(\.\.\.args\)` matches (validator execution)
- [ ] Grep pattern `originalMethod.apply` matches (method passthrough)
</acceptance_criteria>

---

### Task 5: Create Decorator Barrel Export

<read_first>
- bin/lib/decorators/types.ts (after Task 1)
- bin/lib/decorators/LogExecution.ts (after Task 2)
- bin/lib/decorators/CacheResult.ts (after Task 3)
- bin/lib/decorators/ValidateInput.ts (after Task 4)
</read_first>

<action>
Create `bin/lib/decorators/index.ts` with:

1. Export all three decorator functions:
   - `export { LogExecution } from './LogExecution.js'`
   - `export { CacheResult } from './CacheResult.js'`
   - `export { ValidateInput } from './ValidateInput.js'`

2. Export all type definitions:
   - `export type { LogExecutionOptions } from './types.js'`
   - `export type { CacheResultOptions } from './types.js'`
   - `export type { ValidateInputOptions } from './types.js'`
</action>

<acceptance_criteria>
- [ ] File exists: `bin/lib/decorators/index.ts`
- [ ] Grep pattern `export { LogExecution }` matches
- [ ] Grep pattern `export { CacheResult }` matches
- [ ] Grep pattern `export { ValidateInput }` matches
- [ ] Grep pattern `export type { LogExecutionOptions }` matches
- [ ] Grep pattern `export type { CacheResultOptions }` matches
- [ ] Grep pattern `export type { ValidateInputOptions }` matches
</acceptance_criteria>

---

### Task 6: Update tsconfig.json for Decorators

<read_first>
- tsconfig.json (current configuration)
</read_first>

<action>
Update `tsconfig.json` to enable TypeScript decorators:

1. Add to `compilerOptions`:
   ```json
   {
     "experimentalDecorators": true,
     "useDefineForClassFields": true
   }
   ```

2. Verify these settings don't conflict with existing options
3. Run TypeScript compilation to verify no errors: `npm run build`
</action>

<acceptance_criteria>
- [ ] tsconfig.json contains `"experimentalDecorators": true`
- [ ] tsconfig.json contains `"useDefineForClassFields": true`
- [ ] TypeScript compiles without errors: `npm run build` exits with code 0
- [ ] No TypeScript errors about decorator usage
</acceptance_criteria>

---

### Task 7: Apply @CacheResult to ContextCompressor

<read_first>
- bin/lib/context-compressor.ts (current implementation)
- bin/lib/decorators/index.ts (after Task 5)
</read_first>

<action>
Apply `@CacheResult` decorator to `ContextCompressor.compress()` method:

1. Import `CacheResult` from './decorators/index.js'
2. Add decorator above compress method:
   ```typescript
   @CacheResult(
     (content, options) => `compress:${hash(content)}:${JSON.stringify(options)}`,
     600000 // 10 minutes
   )
   async compress(content: string, options: CompressionOptions): Promise<CompressionResult>
   ```

3. Create helper `hash()` function if not exists (simple string hash)
4. Verify decorator is applied correctly
</action>

<acceptance_criteria>
- [ ] Grep pattern `@CacheResult` matches in context-compressor.ts
- [ ] Grep pattern `import.*CacheResult.*from.*decorators` matches
- [ ] compress method has @CacheResult decorator above it
- [ ] Cache key function includes content hash
- [ ] TTL is set (verify with grep: `600000` or similar)
</acceptance_criteria>

---

### Task 8: Apply @CacheResult to ContextRelevanceScorer

<read_first>
- bin/lib/context-relevance-scorer.ts (current implementation)
- bin/lib/decorators/index.ts
</read_first>

<action>
Apply `@CacheResult` decorator to `ContextRelevanceScorer.score()` method:

1. Import `CacheResult` from './decorators/index.js'
2. Add decorator above score method:
   ```typescript
   @CacheResult(
     (content, query) => `score:${hash(content)}:${hash(query)}`,
     600000 // 10 minutes
   )
   async score(content: string, query: string): Promise<number>
   ```

3. Use same hash helper as Task 7
</action>

<acceptance_criteria>
- [ ] Grep pattern `@CacheResult` matches in context-relevance-scorer.ts
- [ ] Grep pattern `import.*CacheResult.*from.*decorators` matches
- [ ] score method has @CacheResult decorator above it
- [ ] Cache key function includes content and query hash
</acceptance_criteria>

---

### Task 9: Apply @LogExecution to ContextDeduplicator

<read_first>
- bin/lib/context-deduplicator.ts (current implementation)
- bin/lib/decorators/index.ts
</read_first>

<action>
Apply `@LogExecution` decorator to `ContextDeduplicator` public methods:

1. Import `LogExecution` from './decorators/index.js'
2. Add decorator to `deduplicate()` method:
   ```typescript
   @LogExecution('ContextDeduplicator.deduplicate', { logParams: false, logResult: false })
   async deduplicate(content: string): Promise<string>
   ```

3. Add decorator to `deduplicateFiles()` method:
   ```typescript
   @LogExecution('ContextDeduplicator.deduplicateFiles', { logParams: false })
   async deduplicateFiles(files: string[]): Promise<string>
   ```
</action>

<acceptance_criteria>
- [ ] Grep pattern `@LogExecution` matches in context-deduplicator.ts
- [ ] Grep pattern `import.*LogExecution.*from.*decorators` matches
- [ ] deduplicate method has @LogExecution decorator
- [ ] deduplicateFiles method has @LogExecution decorator
</acceptance_criteria>

---

### Task 10: Apply @LogExecution to SkillMatcher

<read_first>
- bin/lib/skill-matcher.ts (current implementation)
- bin/lib/decorators/index.ts
</read_first>

<action>
Apply `@LogExecution` and `@CacheResult` decorators to `SkillMatcher` public methods:

1. Import both decorators from './decorators/index.js'
2. Add `@CacheResult` and `@LogExecution` to `match()` method:
   ```typescript
   @CacheResult(
     (query, skills) => `match:${hash(query)}:${skills.length}`,
     300000
   )
   @LogExecution('SkillMatcher.match', { logParams: false })
   async match(query: string, skills: Skill[]): Promise<Skill[]>
   ```

3. Add `@LogExecution` to `matchByTrigger()` and `matchByRelevance()` methods
</action>

<acceptance_criteria>
- [ ] Grep pattern `@CacheResult` matches in skill-matcher.ts
- [ ] Grep pattern `@LogExecution` matches in skill-matcher.ts
- [ ] Grep pattern `import.*LogExecution.*from.*decorators` matches
- [ ] Grep pattern `import.*CacheResult.*from.*decorators` matches
- [ ] match method has both decorators
- [ ] matchByTrigger method has @LogExecution decorator
- [ ] matchByRelevance method has @LogExecution decorator
</acceptance_criteria>

---

### Task 11: Apply @LogExecution to SkillTriggerEvaluator

<read_first>
- bin/lib/skill-triggers.ts (current implementation)
- bin/lib/decorators/index.ts
</read_first>

<action>
Apply `@LogExecution` decorator to `SkillTriggerEvaluator` public methods:

1. Import `LogExecution` from './decorators/index.js'
2. Add decorator to `evaluate()` method:
   ```typescript
   @LogExecution('SkillTriggerEvaluator.evaluate', { logParams: false })
   async evaluate(skill: Skill, context: Context): Promise<boolean>
   ```

3. Add decorator to `evaluateTriggers()` method
</action>

<acceptance_criteria>
- [ ] Grep pattern `@LogExecution` matches in skill-triggers.ts
- [ ] Grep pattern `import.*LogExecution.*from.*decorators` matches
- [ ] evaluate method has @LogExecution decorator
- [ ] evaluateTriggers method has @LogExecution decorator
</acceptance_criteria>

---

### Task 12: Apply @ValidateInput and @LogExecution to SkillValidator

<read_first>
- bin/lib/skill-validator.ts (current implementation)
- bin/lib/decorators/index.ts
</read_first>

<action>
Apply `@ValidateInput` and `@LogExecution` decorators to `SkillValidator` public methods:

1. Import both decorators from './decorators/index.js'
2. Add `@ValidateInput` and `@LogExecution` to `validate()` method:
   ```typescript
   @ValidateInput((skill: Skill) => {
     if (!skill) throw new Error('Skill is required');
     if (!skill.name) throw new Error('Skill name is required');
   })
   @LogExecution('SkillValidator.validate', { logParams: false })
   async validate(skill: Skill): Promise<ValidationResult>
   ```

3. Add `@LogExecution` to `validatePrerequisites()` and `validateTriggers()` methods
</action>

<acceptance_criteria>
- [ ] Grep pattern `@ValidateInput` matches in skill-validator.ts
- [ ] Grep pattern `@LogExecution` matches in skill-validator.ts
- [ ] Grep pattern `import.*ValidateInput.*from.*decorators` matches
- [ ] validate method has both decorators
- [ ] validatePrerequisites method has @LogExecution decorator
- [ ] validateTriggers method has @LogExecution decorator
</acceptance_criteria>

---

### Task 13: Apply @LogExecution to SessionManager

<read_first>
- bin/lib/session-manager.ts (current implementation)
- bin/lib/decorators/index.ts
</read_first>

<action>
Apply `@LogExecution` decorator to `SessionManager` public methods:

1. Import `LogExecution` from './decorators/index.js'
2. Add decorator to `createSession()` method:
   ```typescript
   @LogExecution('SessionManager.createSession', { logParams: true, level: 'info' })
   createSession(sessionId: string): SessionState
   ```

3. Add decorator to `loadState()` and `saveState()` methods
4. Add decorator to `updateState()` method if exists
</action>

<acceptance_criteria>
- [ ] Grep pattern `@LogExecution` matches in session-manager.ts
- [ ] Grep pattern `import.*LogExecution.*from.*decorators` matches
- [ ] createSession method has @LogExecution decorator
- [ ] loadState method has @LogExecution decorator
- [ ] saveState method has @LogExecution decorator
</acceptance_criteria>

---

### Task 14: Apply @LogExecution to SkillContextResolver

<read_first>
- bin/lib/skill-context.ts (current implementation)
- bin/lib/decorators/index.ts
</read_first>

<action>
Apply `@CacheResult` and `@LogExecution` decorators to `SkillContextResolver` public methods:

1. Import both decorators from './decorators/index.js'
2. Add `@CacheResult` and `@LogExecution` to `resolve()` method:
   ```typescript
   @CacheResult(
     (skill: Skill) => `skill-context:${skill.name}`,
     600000
   )
   @LogExecution('SkillContextResolver.resolve', { logParams: false })
   async resolve(skill: Skill): Promise<string>
   ```

3. Add `@LogExecution` to `resolveBatch()` and `clearCache()` methods
</action>

<acceptance_criteria>
- [ ] Grep pattern `@CacheResult` matches in skill-context.ts
- [ ] Grep pattern `@LogExecution` matches in skill-context.ts
- [ ] Grep pattern `import.*CacheResult.*from.*decorators` matches
- [ ] resolve method has both decorators
- [ ] resolveBatch method has @LogExecution decorator
- [ ] clearCache method has @LogExecution decorator
</acceptance_criteria>

---

### Task 15: Update Barrel Export for Decorators

<read_first>
- bin/lib/index.ts (current barrel export)
- bin/lib/decorators/index.ts (after Task 5)
</read_first>

<action>
Update `bin/lib/index.ts` to re-export decorators:

1. Add decorator exports:
   ```typescript
   export { LogExecution, CacheResult, ValidateInput } from './decorators/index.js'
   export type { LogExecutionOptions, CacheResultOptions, ValidateInputOptions } from './decorators/index.js'
   ```

2. Verify exports don't conflict with existing exports
</action>

<acceptance_criteria>
- [ ] File `bin/lib/index.ts` exports `LogExecution`
- [ ] File `bin/lib/index.ts` exports `CacheResult`
- [ ] File `bin/lib/index.ts` exports `ValidateInput`
- [ ] File `bin/lib/index.ts` exports `LogExecutionOptions` type
- [ ] File `bin/lib/index.ts` exports `CacheResultOptions` type
- [ ] File `bin/lib/index.ts` exports `ValidateInputOptions` type
- [ ] Grep pattern `export.*LogExecution.*from.*decorators` matches
</acceptance_criteria>

---

### Task 16: Run Tests and Verify

<read_first>
- vitest.config.ts (test configuration)
- package.json (test scripts)
</read_first>

<action>
Run full test suite to verify decorator application doesn't break existing functionality:

1. Run tests: `npm test`
2. Check test coverage: `npm run test:coverage`
3. Fix any failing tests
4. Verify 70%+ coverage threshold maintained
5. Run lint: `npm run lint`
6. Fix any ESLint warnings
7. Verify build: `npm run build`
</action>

<acceptance_criteria>
- [ ] All tests pass: `npm test` exits with code 0
- [ ] Test coverage >= 70%: `npm run test:coverage` shows >= 70
- [ ] Zero ESLint warnings: `npm run lint` exits with code 0
- [ ] Build succeeds: `npm run build` exits with code 0
- [ ] No runtime errors in test output
- [ ] Decorators are applied (verify with grep across all files: `@CacheResult`, `@LogExecution`, `@ValidateInput`)
</acceptance_criteria>

---

## Verification Criteria

**Goal-backward verification (must_haves):**

- [ ] **CORE-06 addressed:** Decorator pattern implemented for cross-cutting concerns
- [ ] **Three decorators created:** @LogExecution, @CacheResult, @ValidateInput all functional
- [ ] **Decorator infrastructure:** `bin/lib/decorators/` directory with all decorator files
- [ ] **TypeScript configuration:** `experimentalDecorators: true` and `useDefineForClassFields: true` in tsconfig.json
- [ ] **Decorators applied:** At least 8 service classes using decorators
- [ ] **@CacheResult on caching methods:** ContextCompressor, ContextRelevanceScorer, SkillMatcher, SkillContextResolver
- [ ] **@LogExecution on service methods:** All service classes have logging
- [ ] **@ValidateInput on validation methods:** SkillValidator has input validation
- [ ] **Backward compatibility:** All existing exports maintained in bin/lib/index.ts
- [ ] **Tests passing:** All 472+ tests still passing
- [ ] **Coverage maintained:** 70%+ code coverage threshold
- [ ] **Build succeeds:** TypeScript compiles without errors
- [ ] **Zero ESLint warnings:** Code quality maintained

**Implementation order verification:**

- [ ] Task 1 (Types) completed before Tasks 2-4 (decorator implementations)
- [ ] Tasks 2-4 (Decorators) completed before Task 5 (barrel export)
- [ ] Task 6 (tsconfig) completed before Tasks 7-14 (decorator application)
- [ ] Tasks 7-14 (Application) completed in any order
- [ ] Task 15 (Barrel export update) completed after Task 5
- [ ] Task 16 (Tests) completed last as verification

---

## Notes

**Dependencies on other Phase 10 plans:**
- This plan (10.2) depends on Plan 10.1 (CORE-01) for class-based architecture foundation
- Plan 10.3 (Strategy Pattern) will benefit from @CacheResult decorator on compression strategies
- Plan 10.4 (Observer Pattern) is independent
- Plan 10.5 (Adapter Pattern) will use @LogExecution decorator on adapter methods
- Plan 10.6 (Factory Pattern) will use decorators for factory logging/caching
- Plan 10.7 (Facade Pattern) will orchestrate decorated classes

**Token optimization impact:**
- @CacheResult decorator reduces redundant API calls by caching compression/scoring results
- Estimated 20-30% reduction in redundant operations through caching
- @LogExecution provides audit trail without manual logging code

**Risk mitigation:**
- Decorators can be disabled by removing from tsconfig.json if issues arise
- All decorators maintain backward compatibility (wrap existing methods)
- Run full test suite after each decorator application

---

*Plan created: 2026-03-25*
*Phase: 10 | Wave: 10.2 | Requirements: CORE-06*
