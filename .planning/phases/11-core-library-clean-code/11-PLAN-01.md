---
wave: 11.1
depends_on:
  - Phase 10 complete (design patterns infrastructure)
  - jscpd configured
  - Tests passing (baseline verification)
files_modified:
  - bin/lib/**/*.ts
  - .jscpd.json (new)
  - .eslintrc.json (updated)
  - package.json (scripts updated)
autonomous: true
---

# Plan 11.1: DRY Principle — Duplicate Code Elimination (CORE-08)

**Requirement:** CORE-08 — Eliminate duplicate code patterns (DRY principle)

**Goal:** Detect and eliminate duplicate code blocks across the core library, reducing code duplication to zero blocks > 5 lines.

**Implementation Order:** Tooling setup → Detection → Extraction → Validation

---

## Tasks

### Task 11.1-A: Configure Duplicate Detection Tooling

<read_first>
- .planning/phases/11-core-library-clean-code/11-RESEARCH.md (jscpd configuration section)
- package.json (current devDependencies and scripts)
- .eslintrc.json (current ESLint rules)
</read_first>

<action>
1. Create `.jscpd.json` in project root with this exact configuration:
   ```json
   {
     "threshold": 10,
     "minLines": 5,
     "minTokens": 70,
     "ignore": [
       "**/node_modules/**",
       "**/dist/**",
       "**/*.test.ts",
       "**/*.spec.ts"
     ],
     "reporters": ["console", "html"],
     "output": "./reports/jscpd"
   }
   ```

2. Update `.eslintrc.json` to add these rules in the `rules` section:
   - `"complexity": ["error", { "max": 10 }]`
   - `"max-depth": ["error", { "max": 3 }]`
   - `"max-lines-per-function": ["warn", { "max": 50 }]`
   - `"@typescript-eslint/no-unused-vars": "error"`

3. Update `package.json` scripts section to add:
   - `"analyze:duplicates": "jscpd --config .jscpd.json"`
   - `"analyze:complexity": "eslint --rule 'complexity: error' bin/lib/"`

4. Verify jscpd is in devDependencies (it should be from Phase 10)
</action>

<acceptance_criteria>
- File `.jscpd.json` exists at project root with threshold=10, minLines=5, minTokens=70
- `.eslintrc.json` contains `"complexity": ["error", { "max": 10 }]`
- `.eslintrc.json` contains `"max-depth": ["error", { "max": 3 }]`
- `.eslintrc.json` contains `"max-lines-per-function": ["warn", { "max": 50 }]`
- `package.json` contains `"analyze:duplicates": "jscpd --config .jscpd.json"`
- `package.json` contains `"analyze:complexity": "eslint --rule 'complexity: error' bin/lib/"`
- Command `npm run analyze:duplicates` executes without error
</acceptance_criteria>

### Task 11.1-B: Run Baseline Duplicate Analysis

<read_first>
- .planning/phases/11-core-library-clean-code/11-RESEARCH.md (detection strategy)
- bin/lib/index.ts (to understand module structure)
</read_first>

<action>
1. Run `npm run analyze:duplicates` to generate baseline duplicate report
2. Save the HTML report output to `.planning/phases/11-core-library-clean-code/reports/jscpd-baseline.html`
3. Document findings in a file `.planning/phases/11-core-library-clean-code/reports/11.1-DUPLICATES.md` with:
   - Total duplicate blocks found
   - Top 5 most duplicated patterns (file locations, line counts)
   - Estimated duplicated lines percentage
4. Create a prioritized list of duplicates to eliminate (exact blocks 5+ lines, 3+ repetitions first)
</action>

<acceptance_criteria>
- File `.planning/phases/11-core-library-clean-code/reports/11.1-DUPLICATES.md` exists
- Report contains total duplicate blocks count
- Report contains list of top 5 duplicated patterns with file paths
- Report contains prioritized refactoring list
- HTML report saved to `reports/jscpd-baseline.html`
</acceptance_criteria>

### Task 11.1-C: Extract Duplicate Error Handling Patterns

<read_first>
- .planning/phases/11-core-library-clean-code/reports/11.1-DUPLICATES.md (prioritized list)
- bin/lib/error-cache.ts (existing error handling pattern)
- bin/lib/decorators/ValidateInput.ts (decorator infrastructure)
</read_first>

<action>
1. Identify all try-catch blocks that follow this pattern:
   ```typescript
   try {
     // operation
   } catch (error) {
     logger.error('Operation failed', error);
     throw error;
   }
   ```

2. Create a new utility class `bin/lib/utils/OperationHandler.ts` with:
   - Class name: `OperationHandler`
   - Method: `public static async execute<T>(operation: () => Promise<T>, context: string): Promise<T>`
   - The method should wrap the operation in try-catch, log errors with context, and re-throw
   - Add @LogExecution decorator to the execute method
   - Add full TSDoc comments

3. Replace duplicate error handling patterns in these files with OperationHandler.execute():
   - Search for pattern: `catch (error) {` followed by `logger.error` or `console.error`
   - Replace with: `return OperationHandler.execute(async () => { /* original code */ }, 'context')`

4. Update `bin/lib/utils/index.ts` to export OperationHandler
</action>

<acceptance_criteria>
- File `bin/lib/utils/OperationHandler.ts` exists with OperationHandler class
- OperationHandler has `execute<T>(operation: () => Promise<T>, context: string): Promise<T>` method
- OperationHandler is exported from `bin/lib/utils/index.ts`
- Grep search `catch \(error\)` in bin/lib/ returns at least 50% fewer results than baseline
- All replaced error handlers use OperationHandler.execute() pattern
- TypeScript build passes: `npm run build`
</acceptance_criteria>

### Task 11.1-D: Extract Duplicate Configuration Loading

<read_first>
- .planning/phases/11-core-library-clean-code/reports/11.1-DUPLICATES.md
- bin/lib/config-directory-resolver.ts (existing config pattern)
- Entry point files: bin/install.ts, bin/update.ts (for config loading patterns)
</read_first>

<action>
1. Identify all configuration loading patterns that:
   - Read from process.env
   - Load from package.json
   - Parse configuration files

2. Create a new utility class `bin/lib/config/ConfigLoader.ts` with:
   - Class name: `ConfigLoader`
   - Method: `public static getEnvVar(name: string, defaultValue?: string): string`
   - Method: `public static async loadPackageJson(path: string): Promise<any>`
   - Method: `public static async loadConfigFile(path: string): Promise<Record<string, any>>`
   - Add @LogExecution decorator to all methods
   - Add full TSDoc comments

3. Replace duplicate configuration loading in entry points:
   - Replace `process.env.XYZ` with `ConfigLoader.getEnvVar('XYZ')`
   - Replace `require('package.json')` or `import package.json` with `ConfigLoader.loadPackageJson()`
   - Replace file read + JSON.parse with `ConfigLoader.loadConfigFile()`

4. Update `bin/lib/config/index.ts` (create if needed) to export ConfigLoader
</action>

<acceptance_criteria>
- File `bin/lib/config/ConfigLoader.ts` exists with ConfigLoader class
- ConfigLoader has `getEnvVar`, `loadPackageJson`, `loadConfigFile` methods
- ConfigLoader is exported from `bin/lib/config/index.ts`
- Grep search `process\.env\.` in bin/ shows ConfigLoader usage instead of direct access
- Grep search `require\(['"].*package\.json['"]\)` returns 0 results in bin/lib/
- TypeScript build passes: `npm run build`
</acceptance_criteria>

### Task 11.1-E: Extract Duplicate File Operations

<read_first>
- .planning/phases/11-core-library-clean-code/reports/11.1-DUPLICATES.md
- bin/lib/file-operations.ts (existing file operations)
- Entry point files for duplicate file I/O patterns
</read_first>

<action>
1. Search for duplicate file operation patterns:
   - `fs.readFileSync` / `fs.writeFileSync` / `fs.promises.readFile` / `fs.promises.writeFile`
   - `path.join` + file operations
   - File existence checks with `fs.existsSync`

2. Review existing `bin/lib/file-operations.ts` and ensure it has:
   - `readFile(path: string): Promise<string>`
   - `writeFile(path: string, content: string): Promise<void>`
   - `fileExists(path: string): Promise<boolean>`
   - `readJsonFile<T>(path: string): Promise<T>`
   - `writeJsonFile<T>(path: string, data: T): Promise<void>`
   - Add any missing methods
   - Add @LogExecution decorator to all methods

3. Replace duplicate file operations:
   - Replace `fs.promises.readFile()` with `FileOperations.readFile()`
   - Replace `fs.promises.writeFile()` with `FileOperations.writeFile()`
   - Replace `fs.existsSync()` with `FileOperations.fileExists()`
   - Replace `JSON.parse(fs.readFileSync())` with `FileOperations.readJsonFile()`

4. Ensure all entry points use FileOperations instead of direct fs calls
</action>

<acceptance_criteria>
- File `bin/lib/file-operations.ts` contains all 5 methods listed above
- All methods in FileOperations have @LogExecution decorator
- Grep search `fs\.promises\.readFile` in bin/ returns 0 results
- Grep search `fs\.promises\.writeFile` in bin/ returns 0 results
- Grep search `fs\.existsSync` in bin/ returns 0 results
- All file operations go through FileOperations class
- TypeScript build passes: `npm run build`
</acceptance_criteria>

### Task 11.1-F: Eliminate Semantic Duplicates

<read_first>
- .planning/phases/11-core-library-clean-code/reports/11.1-DUPLICATES.md
- CodeComplexityAnalyzer class: bin/lib/code-complexity-analyzer.ts
</read_first>

<action>
1. Run the existing `CodeComplexityAnalyzer.detectDuplicateCode()` to find semantic duplicates:
   ```typescript
   const analyzer = new CodeComplexityAnalyzer(process.cwd());
   const duplicates = await analyzer.detectDuplicateCode();
   ```

2. Identify semantic duplicates (same logic, different variable names/structure)

3. For each semantic duplicate:
   - Extract common logic to utility function or class method
   - Replace all occurrences with calls to the extracted code
   - Ensure extracted code has descriptive name

4. Focus on these common semantic duplicate patterns:
   - Array filtering + mapping patterns
   - Object validation patterns
   - String manipulation patterns
   - Date/time formatting patterns

5. Create utility functions in `bin/lib/fp/` for pure transformations:
   - `bin/lib/fp/array.ts` — Array operations
   - `bin/lib/fp/object.ts` — Object operations
   - `bin/lib/fp/string.ts` — String operations
</action>

<acceptance_criteria>
- CodeComplexityAnalyzer.detectDuplicateCode() runs successfully
- Semantic duplicates reduced by at least 80% from baseline
- New utility files created in `bin/lib/fp/` directory
- Utility functions are pure (no side effects)
- All utility functions have TSDoc comments
- TypeScript build passes: `npm run build`
</acceptance_criteria>

### Task 11.1-G: Validate DRY Refactoring

<read_first>
- .planning/phases/11-core-library-clean-code/reports/11.1-DUPLICATES.md (baseline)
- .planning/REQUIREMENTS.md (CORE-08 success criteria)
</read_first>

<action>
1. Run `npm run analyze:duplicates` to generate post-refactoring report
2. Compare with baseline report from Task 11.1-B
3. Create validation report `.planning/phases/11-core-library-clean-code/reports/11.1-VALIDATION.md` with:
   - Baseline duplicate blocks count
   - Post-refactoring duplicate blocks count
   - Percentage reduction
   - List of any remaining duplicates (with justification if kept intentionally)
4. Verify target metrics:
   - Zero duplicate code blocks > 5 lines with 3+ repetitions
   - Duplicated lines percentage < 1%
5. Run full test suite: `npm test`
6. Run TypeScript build: `npm run build`
</action>

<acceptance_criteria>
- File `.planning/phases/11-core-library-clean-code/reports/11.1-VALIDATION.md` exists
- Validation report shows at least 80% reduction in duplicate blocks
- Validation report shows duplicated lines < 1%
- All tests pass: `npm test` exits with code 0
- TypeScript build succeeds: `npm run build` exits with code 0
- jscpd report shows zero blocks with 100% exact match
</acceptance_criteria>

---

## Verification Criteria

**DRY Principle (CORE-08) Complete When:**
- [ ] jscpd configured with threshold=10, minLines=5, minTokens=70
- [ ] Baseline duplicate report generated and documented
- [ ] OperationHandler utility class created and in use
- [ ] ConfigLoader utility class created and in use
- [ ] FileOperations class has all required methods with decorators
- [ ] Semantic duplicates eliminated using FP utilities
- [ ] Duplicate blocks reduced by at least 80% from baseline
- [ ] Duplicated lines percentage < 1%
- [ ] All tests passing
- [ ] TypeScript build succeeds

**Must Haves (Goal-Backward Verification):**
- CORE-08 requirement satisfied: Duplicate code patterns eliminated
- Tooling in place for ongoing duplicate detection
- Extracted utilities follow OOP+FP hybrid architecture
- Zero breaking changes to public APIs
- All extracted code has TSDoc documentation

**Metrics:**
- Baseline duplicate blocks: (to be measured in Task 11.1-B)
- Target duplicate blocks: 0 exact matches, < 1% duplicated lines
- Estimated code reduction: 20-30% from duplicate elimination

---

*Plan created: 2026-03-26*
*Phase: 11.1 — DRY Principle (CORE-08)*
*Next: Plan 11.2 — KISS Principle (CORE-09)*
