import { CodebaseAnalyzer } from '../../bin/lib/analyzer/codebase-analyzer.js';describe('CodebaseAnalyzer', () => {  let tmpDir: string;  let instance: CodebaseAnalyzer;  beforeEach(() => {    tmpDir = createTempProject();    instance = new CodebaseAnalyzer(tmpDir);  });  afterEach(() => cleanup(tmpDir));  test('constructor does not throw', () => {    expect(instance).toBeTruthy();  });  test('instance methods work', () => {    expect(instance).toBeDefined();  });});