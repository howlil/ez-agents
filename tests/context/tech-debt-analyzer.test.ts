import { TechDebtAnalyzer } from '../../bin/lib/analyzer/tech-debt-analyzer.js';describe('TechDebtAnalyzer', () => {  let tmpDir: string;  let instance: TechDebtAnalyzer;  beforeEach(() => {    tmpDir = createTempProject();    instance = new TechDebtAnalyzer(tmpDir);  });  afterEach(() => cleanup(tmpDir));  test('constructor does not throw', () => {    expect(instance).toBeTruthy();  });  test('instance methods work', () => {    expect(instance).toBeDefined();  });});