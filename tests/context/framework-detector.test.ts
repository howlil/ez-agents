import { FrameworkDetector } from '../../bin/lib/detector/framework-detector.js';describe('FrameworkDetector', () => {  let tmpDir: string;  let instance: FrameworkDetector;  beforeEach(() => {    tmpDir = createTempProject();    instance = new FrameworkDetector(tmpDir);  });  afterEach(() => cleanup(tmpDir));  test('constructor does not throw', () => {    expect(instance).toBeTruthy();  });  test('instance methods work', () => {    expect(instance).toBeDefined();  });});