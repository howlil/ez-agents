import { ConstraintExtractor } from '../../bin/lib/detector/constraint-extractor.js';describe('ConstraintExtractor', () => {  let tmpDir: string;  let instance: ConstraintExtractor;  beforeEach(() => {    tmpDir = createTempProject();    instance = new ConstraintExtractor(tmpDir);  });  afterEach(() => cleanup(tmpDir));  test('constructor does not throw', () => {    expect(instance).toBeTruthy();  });  test('instance methods work', () => {    expect(instance).toBeDefined();  });});