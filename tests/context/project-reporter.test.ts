import { fileURLToPath } from 'url';/** * Project Reporter Tests * Tests for ProjectReporter class */import { strict as assert } from 'node:assert';import path from 'node:path';import { describe, it, beforeAll } from 'vitest';import { ProjectReporter } from '../../bin/lib/reporter/project-reporter.js';const testRoot = path.join(__dirname, '..', 'fixtures', 'test-project');describe('ProjectReporter', () => {  let reporter;  beforeAll(() => {    reporter = new ProjectReporter(testRoot);  });  it('generate returns complete report with all sections', () => {    const structure = {      root: testRoot,      directories: [{ path: path.join(testRoot, 'src'), depth: 1 }],      entryPoints: [path.join(testRoot, 'src', 'index.ts')],      configFiles: [path.join(testRoot, 'package.json')],      sourceDirs: [path.join(testRoot, 'src')],      testDirs: [],      files: [path.join(testRoot, 'src', 'index.ts')],      modules: [{ name: 'components', path: path.join(testRoot, 'src', 'components'), fileCount: 5 }]    };    const stack = {      language: 'typescript',      runtime: 'node',      packageManager: 'pnpm',      frameworks: ['React', 'Next.js'],      databases: ['PostgreSQL'],      infrastructure: ['Sentry']    };    const techDebt = {      findings: [        { file: 'src/old.ts', line: 10, severity: 'High', description: 'Old code' },        { file: 'src/complex.ts', line: 5, severity: 'Medium', type: 'complexity', description: 'Complex function' }      ]    };    const report = reporter.generate(structure, stack, techDebt);    expect(report).toBeTruthy()    expect(report.includes('# Project Analysis Report')).toBeTruthy()    expect(report.includes('## File Structure')).toBeTruthy()    expect(report.includes('## Technology Stack')).toBeTruthy()    expect(report.includes('## Architecture Overview')).toBeTruthy()    expect(report.includes('## Pain Points')).toBeTruthy()    expect(report.includes('## Recommendations')).toBeTruthy()  });  it('buildArchitectureOverview includes pattern summary', () => {    const structure = {      root: testRoot,      directories: [        { path: path.join(testRoot, 'src', 'components'), depth: 2 },        { path: path.join(testRoot, 'src', 'pages'), depth: 2 }      ],      entryPoints: [],      modules: []    };    const stack = { frameworks: ['Next.js'] };    const overview = reporter.buildArchitectureOverview(structure, stack);    expect(overview.includes('Pattern')).toBeTruthy()    expect(overview.includes('Next.js') || overview.includes('Component')).toBeTruthy()  });  it('buildArchitectureOverview includes layer descriptions', () => {    const structure = {      root: testRoot,      directories: [        { path: path.join(testRoot, 'src', 'components'), depth: 2 },        { path: path.join(testRoot, 'src', 'services'), depth: 2 },        { path: path.join(testRoot, 'src', 'models'), depth: 2 }      ],      entryPoints: [],      modules: []    };    const stack = {};    const overview = reporter.buildArchitectureOverview(structure, stack);    expect(overview.includes('Layers')).toBeTruthy()  });  it('buildPainPoints sorts issues by severity', () => {    const techDebt = {      findings: [        { file: 'a.ts', severity: 'Low', description: 'Low issue' },        { file: 'b.ts', severity: 'Critical', description: 'Critical issue' },        { file: 'c.ts', severity: 'High', description: 'High issue' },        { file: 'd.ts', severity: 'Medium', description: 'Medium issue' }      ]    };    const painPoints = reporter.buildPainPoints(techDebt);    expect(painPoints.includes('Pain Points')).toBeTruthy()    // Critical should appear before High in the output    const criticalIndex = painPoints.indexOf('Critical');    const highIndex = painPoints.indexOf('High');    if (criticalIndex >= 0 && highIndex >= 0) {      expect(criticalIndex < highIndex).toBeTruthy()    }  });  it('buildRecommendations includes effort estimates', () => {    const techDebt = {      findings: [        { file: 'large.ts', severity: 'High', type: 'large_file', lines: 600 },        { file: 'complex.ts', severity: 'Medium', type: 'complexity' }      ]    };    const stack = { frameworks: ['Next.js'] };    const recommendations = reporter.buildRecommendations(techDebt, stack);    expect(recommendations.includes('Recommendations')).toBeTruthy()    expect(recommendations.includes('Effort:')).toBeTruthy()  });  it('buildFileStructureSummary creates directory tree', () => {    const structure = {      root: testRoot,      directories: [        { path: path.join(testRoot, 'src'), depth: 1 },        { path: path.join(testRoot, 'tests'), depth: 1 },        { path: path.join(testRoot, 'docs'), depth: 1 }      ],      entryPoints: [path.join(testRoot, 'src', 'index.ts')],      configFiles: [path.join(testRoot, 'package.json')],      modules: []    };    const summary = reporter.buildFileStructureSummary(structure);    expect(summary.includes('File Structure')).toBeTruthy()    expect(summary.includes('Directory Layout')).toBeTruthy()  });  it('buildTechStackSummary lists languages, frameworks, databases', () => {    const stack = {      language: 'typescript',      runtime: 'node',      packageManager: 'pnpm',      frameworks: ['React', 'Next.js'],      databases: ['PostgreSQL', 'Redis'],      infrastructure: ['Sentry', 'AWS SDK']    };    const summary = reporter.buildTechStackSummary(stack);    expect(summary.includes('Technology Stack')).toBeTruthy()    expect(summary.includes('typescript')).toBeTruthy()    expect(summary.includes('React')).toBeTruthy()    expect(summary.includes('PostgreSQL')).toBeTruthy()  });  it('generate handles empty data gracefully', () => {    const structure = { root: testRoot, directories: [], entryPoints: [], configFiles: [], files: [], modules: [] };    const stack = { language: 'unknown', runtime: 'unknown', frameworks: [], databases: [], infrastructure: [] };    const techDebt = { findings: [] };    const report = reporter.generate(structure, stack, techDebt);    expect(report).toBeTruthy()    expect(report.length > 0).toBeTruthy()  });  it('buildPainPoints handles empty findings', () => {    const techDebt = { findings: [] };    const painPoints = reporter.buildPainPoints(techDebt);    expect(painPoints.includes('Pain Points')).toBeTruthy()  });  it('buildRecommendations handles empty findings', () => {    const techDebt = { findings: [] };    const stack = {};    const recommendations = reporter.buildRecommendations(techDebt, stack);    expect(recommendations.includes('Recommendations')).toBeTruthy()  });});