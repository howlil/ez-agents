import { DependencyGraph } from '../../bin/lib/analyzer/dependency-graph.js';describe('DependencyGraph', () => {  let tmpDir: string;  let instance: DependencyGraph;  beforeEach(() => {    tmpDir = createTempProject();    instance = new DependencyGraph(tmpDir);  });  afterEach(() => cleanup(tmpDir));  test('constructor does not throw', () => {    expect(instance).toBeTruthy();  });  test('instance methods work', () => {    expect(instance).toBeDefined();  });});